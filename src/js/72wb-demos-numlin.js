const NUMLIN_GROUPS = [
{ g:'PA = LU — and the two different ways a solve goes wrong', items:[
  {n:'Elimination, stored rather than thrown away', ex:'PA = LU', stage:'nlFact',
   opts:{key:'poisson'},
   out:'The permutation, L and U side by side, with ‖PA − LU‖ at round-off against ‖A‖ — and x reached twice, once by forward and back substitution through the factors and once by the row reduction in the linear-systems wing. On the Poisson matrix no row is ever swapped and the growth factor is exactly 1 — not one entry anywhere in the elimination is larger than the largest entry of A.',
   note:'The row operations of Gaussian elimination were a matrix multiplication all along, and writing them down instead of discarding them costs nothing and returns a factorisation. That is worth more than the answer: a second right-hand side then costs n² instead of n³, and the determinant falls out as the product of the pivots. Every library returns the factors for exactly this reason.'},
  {n:'A pivot of 10⁻¹⁷, and an answer that is simply wrong', ex:'κ = 3.7, and no correct digits', stage:'nlFact',
   opts:{key:'tiny', pivot:false},
   out:'With pivoting off, x comes back with its first component nowhere near 1 — and there is no warning of any kind: no exception, no error raised, no large residual. Tick the pivoting box and the same code is exact to sixteen digits.',
   note:'The first pivot is not zero, so unpivoted elimination proceeds happily and divides by 10⁻¹⁷. The multipliers reach 10¹⁷, and multiplying a row by 10¹⁷ multiplies its rounding errors by 10¹⁷ too, so the information in the other rows is destroyed — not approximated, destroyed. Look at the condition number while you do it: <b>κ is 3.7</b>. The problem is easy. The algorithm was the entire failure, and that is a distinction the rest of this wing is built on.'},
  {n:'Where the answer is wrong and nothing can fix it', ex:'Hilbert 5×5, κ = 4.8×10⁵', stage:'nlFact',
   opts:{key:'hilb'},
   out:'Pivoting on, growth factor 1, residual at 10⁻¹⁷ — a flawless factorisation. The error in x is 3.5×10⁻¹³ — three and a half orders of magnitude larger than the residual, on a matrix whose κ is 4.8×10⁵.',
   note:'The exact mirror of the preset before it. Here the algorithm does everything right and the answer is still poor, because the matrix genuinely is that sensitive: a Hilbert matrix differing from this one in the sixteenth digit has a visibly different solution. No amount of pivoting, and no amount of extra precision, changes that — only a different question does.'},
  {n:'The matrix that defeats partial pivoting', ex:'growth 2ⁿ⁻¹, and not one swap', stage:'nlFact',
   opts:{key:'growth'},
   out:'Every multiplier is exactly 1, partial pivoting performs zero swaps because every pivot is already the largest in its column, and the growth factor still reaches 64 at n = 7 — doubling with every row.',
   note:'Pivoting bounds the <i>multipliers</i> by 1, which is not the same as bounding the matrix, and Wilkinson\'s example is the standing counterexample to the belief that they are. Partial pivoting is used universally because a growth factor anywhere near 2ⁿ⁻¹ essentially never occurs on matrices that arise from real problems — it is a bet with excellent odds and a known losing hand, not a theorem.'},
  {n:'A zero in the corner, which is the easy case', ex:'a₁₁ = 0', stage:'nlFact',
   opts:{key:'swap'},
   out:'One row swap, and the factorisation proceeds. The permutation row of the readout names which rows moved.',
   note:'This is pivoting\'s obvious job and the one every textbook shows: without a swap the first division is by zero and there is nothing to argue about. The interesting cases are the two presets before this one, where nothing is zero, nothing raises, and the answer is wrong anyway — those are what pivoting is really for.'}
]},
{ g:'Building an orthogonal basis, three ways', items:[
  {n:'Two spellings of Gram–Schmidt that are the same in exact arithmetic', ex:'‖QᵀQ − I‖ against κ', stage:'nlQR',
   opts:{n:8, lk:5, method:'cgs'},
   out:'At κ = 10⁵ the classical form has lost 6×10⁻⁹ of its orthogonality and the modified form 7×10⁻¹³; both reconstruct A to 10⁻¹⁶. The difference between them is one line of code — whether each projection is taken against the original column or against what is left of it.',
   note:'Once q₁ has been subtracted off, the remainder is orthogonal to q₁, so the two forms compute the same coefficient — <i>exactly</i>, in exact arithmetic. In floating point the computed q₁ is only orthogonal to within ε, and when the new column is nearly in the span of the old ones almost all of its digits cancel, leaving that ε in charge. The classical form takes every later coefficient against the uncontaminated original and so never corrects for it; the modified form takes each against the current remainder and removes the error the previous step introduced. One extra bite turns κ² into κ.'},
  {n:'The exponent, measured rather than quoted', ex:'slope of log‖QᵀQ − I‖ vs log κ', stage:'nlQR',
   opts:{n:8, lk:6, method:'cgs'},
   out:'A least-squares fit over five decades of κ gives 2.09 for classical Gram–Schmidt, 0.79 for modified, and −0.03 for Householder, against predicted slopes of 2, 1 and 0.',
   note:'Two things to take from those three numbers. The classical bound is <b>tight</b> — the measurement lands on 2. The modified bound is <b>not attained</b> on this family, coming out below 1, and saying so is the point: εκ is an upper bound, and an upper bound being met is a fact about the example rather than a property of the method. The fit deliberately starts at κ = 10³, because below that the classical error is already at the machine-ε floor, and a curve that has bottomed out cannot be fitted through — including those points dragged the first version of this measurement to 1.63.'},
  {n:'Householder, which does not care about κ at all', ex:'‖QᵀQ − I‖ ≈ ε, flat', stage:'nlQR',
   opts:{n:10, lk:7, method:'hh'},
   out:'A horizontal line at 10⁻¹⁶ across seven decades of condition number. At κ = 10⁷ the classical method has lost every digit past the fourth and Householder has lost none.',
   note:'A reflection I − 2vvᵀ/vᵀv is orthogonal because of its <i>form</i>, not because of the data it was built from — so the computed reflector is orthogonal to within ε whatever v is, and Q, being a product of a few dozen of them, inherits that. The departure from orthogonality is bounded by the number of columns and has no dependence on the conditioning at all. It costs about twice the flops of Gram–Schmidt, and it is what every library does.'},
  {n:'All three factor A perfectly, which is why this is a trap', ex:'‖A − QR‖ ≈ 10⁻¹⁶ for all three', stage:'nlQR',
   opts:{n:12, lk:8, method:'mgs'},
   out:'At κ = 10⁸ the reconstruction residual is at round-off for every one of the three methods, while their orthogonality errors span 10⁻¹⁶ to 10⁻³.',
   note:'A check that stopped at "does QR give back A" would pass all three and find nothing. The property everything downstream actually uses is that Qᵀ is Q\'s inverse: a least-squares solve does Qᵀb and then a triangular back-substitution, and if Q is not orthogonal that first step is not a projection and the answer is to a different problem. This is the general shape of a stability failure — the quantity you naturally check is fine, and the quantity you assumed is not.'}
]},
{ g:'Conditioning — how much the answer is allowed to move', items:[
  {n:'A matrix takes the circle to an ellipse, always', ex:'κ = σ₁/σ₂', stage:'nlCond',
   opts:{view:'ellipse', ckey:'thin'},
   out:'The unit circle dashed, its image drawn solid, and the two semi-axes as arrows of length σ₁ and σ₂. The condition number is the ratio of the arrows, read off the picture; the area factor |det A| is checked against σ₁σ₂ and they agree to every digit.',
   note:'This is what the condition number <i>is</i>, before any inequality: the eccentricity of the ellipse. A matrix can have a perfectly healthy determinant and a terrible κ — stretch one axis by a thousand and squash the other by a thousand and the area is unchanged — which is why the determinant is useless as a measure of how close to singular a matrix is, and κ is not.'},
  {n:'Nearly singular, drawn', ex:'rows differing in the fourth decimal', stage:'nlCond',
   opts:{view:'ellipse', ckey:'nearly'},
   out:'An ellipse so flat it reads as a line segment. κ is about 10⁴, and the worst perturbation of b is amplified by exactly that.',
   note:'The picture explains the danger better than the algebra does. A short step <i>across</i> the flattened direction is the image of an enormous step in the pre-image, so a tiny movement of b demands a huge movement of x. Edit the 1.0004 in the boxes towards 1 and watch the ellipse close and κ run away — the matrix is invertible right up to the instant it is not.'},
  {n:'The bound is attained, which is why it is worth quoting', ex:'‖δx‖/‖x‖ ≤ κ ‖δb‖/‖b‖', stage:'nlCond',
   opts:{view:'amplify', an:6},
   out:'Ten condition numbers from 10 to 10¹⁰, each with a right-hand side perturbed by one part in a billion. The worst direction lands exactly on the diagonal — amplification equals κ, to every digit — while four ordinary directions scatter well below it.',
   note:'Take b along u₁, the direction A stretches most, so x is as short as it can be; take δb along uₙ, the direction A stretches least, so A⁻¹δb is as long as it can be. Both extremes at once and the inequality becomes an equality. A bound nobody could reach would say nothing about how bad things get; this one is reached. The scattered points matter too — a randomly chosen perturbation usually does far less damage, which is how an ill-conditioned system behaves impeccably for months and then, on one particular right-hand side, does not.'},
  {n:'A small residual is not a small error', ex:'residual 10⁻¹⁶, error 10⁻⁴', stage:'nlCond',
   opts:{view:'resid', hn:12},
   out:'Two curves from the same solutions. The relative residual is flat along the bottom at 10⁻¹⁶ for every size of Hilbert matrix. The relative error climbs by more than an order of magnitude per row and passes 10⁻⁴ by n = 10, staying just under the κ·ε ceiling drawn dashed beside it.',
   note:'A backward-stable solver returns an x̂ that exactly solves a problem whose matrix differs from yours in the sixteenth digit — always, on every matrix there is. So the residual measures whether the algorithm did its job, and the condition number is the exchange rate between that and whether the answer is right. This is the single most expensive misunderstanding in applied linear algebra: "my residual is 10⁻¹⁴, so my answer is good" is not an argument, and this plot is the counterexample.'},
  {n:'Where nothing at all survives', ex:'Hilbert 13×13, κ ≈ 10¹⁸', stage:'nlCond',
   opts:{view:'resid', hn:13},
   out:'By n = 13 the relative error is 2.0 — larger than the answer. The computed answer is not a poor approximation to the vector of ones — it is unrelated to it, and only knowing the answer in advance reveals that.',
   note:'κ has passed 1/ε, which is the point at which the matrix is <i>numerically singular</i>: the rounding of its own entries into float64 already moves the solution completely. Nothing is wrong with the code and nothing better exists in double precision. The honest options are all changes to the question — rescale, change basis (fitting in Chebyshev form instead of powers of x turns exactly this matrix into a well-conditioned one), regularise, or carry more digits and accept that they buy resolution rather than certainty.'}
]},
{ g:'Iterating instead of eliminating', items:[
  {n:'The rate, from a spectrum and from a run', ex:'ρ(G) = cos(π/(n+1))', stage:'nlIter',
   opts:{key:'poisson', n:12, w:1},
   out:'Jacobi\'s spectral radius is 0.9709 from Gelfand\'s formula on the iteration matrix, 0.9709 from a power iteration on the same matrix, and 0.97006 fitted to the error history of a run that never forms it — against a closed form of cos(π/13).',
   note:'Three routes with almost nothing in common. Gelfand\'s formula reads ‖Gᵐ‖^(1/m) off ten repeated squarings and never asks for an eigenvector; the power iteration finds the dominant eigenvalue and is quietly wrong whenever that eigenvalue is a complex pair, which is why it is kept as a cross-check rather than as the answer; and the fitted rate comes from sweeping the equations one at a time. When three routes like that agree, the number is not an artefact of any of them.'},
  {n:'Gauss–Seidel squares the Jacobi rate', ex:'ρ(GS) = ρ(J)²', stage:'nlIter',
   opts:{key:'poisson', n:16, w:1, sweeps:120},
   out:'0.98297 for Jacobi and 0.96624 for Gauss–Seidel — the square, to ten digits — so Gauss–Seidel does in one sweep what Jacobi does in two, and the two error curves have exactly that ratio of slopes.',
   note:'This is Young\'s theorem, and it needs a hypothesis: the matrix must be <i>consistently ordered</i>, which the Poisson matrix is. It is not a general fact about the two methods, and the next two experiments are the counterexamples. Both radii are computed here rather than quoted, so the identity is measured on the matrix in front of you.'},
  {n:'Jacobi finishes in three sweeps; Gauss–Seidel runs away', ex:'ρ(J) = 0, ρ(GS) > 1', stage:'nlIter',
   opts:{key:'jacOnly', sweeps:30},
   out:'The Jacobi curve drops off the bottom of the plot in three sweeps — its iteration matrix is nilpotent, so the spectral radius is exactly zero — while the Gauss–Seidel curve climbs steadily off the top.',
   note:'Neither behaviour is visible by looking at the matrix. It is not diagonally dominant, so the textbook guarantee says nothing — which is not the same as saying it fails, and the difference between those two sentences is this entire experiment. A sufficient condition tells you nothing when its hypothesis is absent.'},
  {n:'And exactly the other way round', ex:'ρ(J) > 1, ρ(GS) < 1', stage:'nlIter',
   opts:{key:'gsOnly', sweeps:30},
   out:'Jacobi diverges and Gauss–Seidel converges, on a matrix as small and as innocent as the last one.',
   note:'The pair is the point. "Gauss–Seidel is Jacobi, only faster" is false, and so is its converse: they are different iterations with different spectra, and which of them converges is a property of the matrix that has to be computed rather than guessed. Two 3×3 matrices are enough to break the rule of thumb in both directions.'},
  {n:'Over-relaxation, and the optimum found by looking', ex:'ω_opt = 2/(1 + √(1 − μ²))', stage:'nlIter',
   opts:{key:'poisson', n:20, w:1.73, sweeps:80},
   out:'The right-hand plot is the spectral radius against ω, with a sharp minimum. At n = 20 Young\'s closed form gives ω_opt = 1.7406 and a golden-section search on the measured ρ(ω) curve — which knows nothing about Young — lands on the same number; the radius there is ω_opt − 1, and SOR reaches 10⁻⁸ in a small fraction of the sweeps Gauss–Seidel needs.',
   note:'Overshooting the Gauss–Seidel step by a factor ω turns an O(n²) sweep count into an O(n) one, for no extra work per sweep — the single best return on one parameter anywhere in this wing. Note the shape of the curve: gentle to the left of the optimum and a straight line ω − 1 to the right, reaching 1 at ω = 2 where convergence stops entirely. An ω guessed slightly high costs far more than one guessed slightly low, which is why every practical recommendation errs low.'}
]},
{ g:'Krylov — the method that made large problems possible', items:[
  {n:'Conjugate gradients against steepest descent', ex:'√κ against κ', stage:'nlKrylov',
   opts:{n:20, steps:60},
   out:'Three curves on the same problem. Conjugate gradients reaches 10⁻⁶ in about 20 steps; steepest descent and Gauss–Seidel are still above 10⁻² when it does. Both bounds are drawn dashed and neither run crosses its own.',
   note:'Both methods cost one matrix–vector product per step, so the difference is not work but memory. Steepest descent minimises along a line, which makes each new residual perpendicular to the last direction and guarantees that the next step partly undoes the previous one — the zig-zag across a long thin valley. Conjugate gradients minimises over the whole space of everything it has seen, so no step can undo an earlier one, and the error in the A-norm falls monotonically.'},
  {n:'The bound checked, not illustrated', ex:'2((√κ−1)/(√κ+1))ᵏ', stage:'nlKrylov',
   opts:{n:30, steps:80},
   out:'The largest amount by which any CG step exceeds its bound, over all eighty steps, is reported as 0 against the bound itself — no step is outside it — and the A-norm error is confirmed to fall at every single step.',
   note:'The error after k steps is p(A)e₀ for some degree-k polynomial with p(0) = 1, and CG picks the best such polynomial automatically. Bounding the best by a Chebyshev polynomial on [λ_min, λ_max] produces the factor above, and the square root appears because Chebyshev polynomials grow fastest just outside the interval where they are small. Steepest descent is the same estimate with the degree pinned at one, and gets κ rather than √κ.'},
  {n:'It stops after n steps, and nobody uses it that way', ex:'eₙ = 0 in exact arithmetic', stage:'nlKrylov',
   opts:{n:12, steps:20},
   out:'At n = 12 the error at step 12 is at round-off, and the dashed vertical marks it. The curve flattens there because there is nothing left to remove.',
   note:'The search space grows by one dimension per step, so after n steps it is all of ℝⁿ and the minimiser over it is the answer — conjugate gradients is technically a direct method. Nobody runs it that way: n steps cost more than an LU factorisation, and in floating point the orthogonality the argument depends on decays, so the termination is not exact either. What makes it the workhorse of large-scale computing is the other property — a good answer long before step n, in a count set by √κ rather than n, with a few vectors of storage and no need for the matrix itself, only the ability to multiply by it.'},
  {n:'Why every improvement is an improvement in κ', ex:'√κ grows like n', stage:'nlKrylov',
   opts:{n:40, steps:120},
   out:'At n = 40 the condition number is 681 and √κ is 26.1 — and CG needs about that many steps, while steepest descent needs hundreds. Raise n and the ratio between them climbs like √κ.',
   note:'κ for this matrix grows like n², so √κ grows like n: the step count is proportional to the number of grid points along one side, however many dimensions the grid has. That single fact is why the literature on large sparse systems is almost entirely about <b>preconditioners</b> and hardly at all about the iteration itself, which has not changed since 1952. Solve M⁻¹Ax = M⁻¹b with M something cheap resembling A, and the count falls like the square root of the new condition number.'}
]}
];
