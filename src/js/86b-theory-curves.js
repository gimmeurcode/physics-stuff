const THEORY_CURVES = `
<div class="toc">
  <a href="#c1">Parametrisation</a><a href="#c2">Calculus of parametric curves</a><a href="#c3">Arc length</a>
  <a href="#c4">Polar coordinates</a><a href="#c5">Area &amp; length in polar</a><a href="#c6">Conic sections</a>
  <a href="#c7">Vector-valued functions</a><a href="#c8">Curvature &amp; the TNB frame</a><a href="#c9">Motion</a>
</div>

<h3 id="c1">A curve is a journey, not a set of points</h3>
<p>A parametrisation is a function <span class="mth"><i>t</i> ↦ (<i>x</i>(<i>t</i>), <i>y</i>(<i>t</i>))</span>, and it carries strictly more information than the track it traces: the same set of points can be run at different speeds, in different directions, and more than once. The calculus knows the difference, and that is the point of the subject rather than a nuisance.</p>
<p>The immediate gain is that curves failing the vertical line test become ordinary. A circle needs two branches as a graph and none as a parametrisation. The folium <span class="mth"><i>x</i>³ + <i>y</i>³ = 3<i>axy</i></span> fails it twice over and is a single smooth parametrised curve. This is why the subject exists.</p>

<h3 id="c2">Slopes, from the chain rule</h3>
<p>If both coordinates depend on <span class="mth"><i>t</i></span>, then <span class="mth"><i>dy</i>/<i>dt</i> = (<i>dy</i>/<i>dx</i>)(<i>dx</i>/<i>dt</i>)</span>, so</p>
<div class="eqb"><span class="mth"><span class="frac"><span class="nm"><i>dy</i></span><span class="den"><i>dx</i></span></span> <span class="op">=</span> <span class="frac"><span class="nm"><i>dy</i>/<i>dt</i></span><span class="den"><i>dx</i>/<i>dt</i></span></span> , &nbsp;&nbsp; provided <i>dx</i>/<i>dt</i> ≠ 0</span></div>
<p>The second derivative is the step everybody drops. It is <em>not</em> the ratio of second derivatives. It is the <span class="mth"><i>t</i></span>-derivative of the <em>slope</em>, divided by <span class="mth"><i>dx</i>/<i>dt</i></span> once more:</p>
<div class="eqb"><span class="mth"><span class="frac"><span class="nm"><i>d</i>²<i>y</i></span><span class="den"><i>dx</i>²</span></span> <span class="op">=</span> <span class="frac"><span class="nm"><i>x</i>′<i>y</i>″ <span class="op">−</span> <i>y</i>′<i>x</i>″</span><span class="den">(<i>x</i>′)³</span></span></span></div>
<p>The numerator is the same cross product that gives the curvature; the denominator is what turns it into a rate with respect to <span class="mth"><i>x</i></span>. The lab prints both derivatives at every <span class="mth"><i>t</i></span>.</p>
<p class="note"><strong>Cusps.</strong> A smooth parametrisation can only produce a corner where <span class="mth"><b>r</b>′(<i>t</i>) = 0</span> — where the journey momentarily stops. The cycloid's cusps sit exactly at the instants when the rolling wheel's contact point is at rest; the astroid has four. The lab locates those instants by root-finding on <span class="mth">|<b>r</b>′|²</span> and marks them, so the cusps are found rather than drawn in.</p>

<h3 id="c3">Arc length and surfaces of revolution</h3>
<p>Pythagoras on an infinitesimal step gives <span class="mth"><i>ds</i>² = <i>dx</i>² + <i>dy</i>²</span>, so arc length is the integral of speed:</p>
<div class="eqb"><span class="mth"><i>L</i> <span class="op">=</span> ∫<sub><i>a</i></sub><sup><i>b</i></sup> √<span class="rad">(<i>x</i>′)² <span class="op">+</span> (<i>y</i>′)²</span> <i>dt</i></span></div>
<p>It belongs to the curve rather than the journey: run the same track twice as fast and the integrand doubles while the interval halves, and the integral is unchanged. Revolving about an axis multiplies each element of length by the circumference it sweeps:</p>
<div class="eqb"><span class="mth"><i>S</i> <span class="op">=</span> 2π ∫ <i>y</i> √<span class="rad">(<i>x</i>′)² <span class="op">+</span> (<i>y</i>′)²</span> <i>dt</i></span></div>
<p>Almost every arc-length integrand is irrational, which is why so few curves have elementary lengths. The ellipse is the famous casualty — its arc length is <em>the</em> elliptic integral, an entire branch of nineteenth-century mathematics grew out of that failure, and the lab checks Ramanujan's 1914 approximation against honest quadrature to about one part in <span class="mth">10⁸</span>.</p>

<h3 id="c4">Polar coordinates</h3>
<p>A point is named by its distance from the origin and the angle from the positive <span class="mth"><i>x</i></span>-axis. Unlike Cartesian coordinates this is not a bijection: every point has infinitely many names, the origin has all of them, and <strong>negative <span class="mth"><i>r</i></span> is allowed</strong> — it means "that far in the opposite direction". That convention is what draws the inner loop of a limaçon, and it is a genuine part of the system rather than a mistake.</p>
<p>The slope of a polar curve is not <span class="mth"><i>dr</i>/<i>dθ</i></span>. Both coordinates depend on <span class="mth">θ</span> through <span class="mth"><i>r</i></span> and through the trigonometric factor, so the product rule gives</p>
<div class="eqb"><span class="mth"><span class="frac"><span class="nm"><i>dy</i></span><span class="den"><i>dx</i></span></span> <span class="op">=</span> <span class="frac"><span class="nm"><i>r</i>′ sin θ <span class="op">+</span> <i>r</i> cos θ</span><span class="den"><i>r</i>′ cos θ <span class="op">−</span> <i>r</i> sin θ</span></span></span></div>
<p>At the origin, where <span class="mth"><i>r</i> = 0</span>, this collapses to <span class="mth">tan θ</span>: the curve leaves the pole along the ray it arrived on.</p>

<h3 id="c5">Area and length in polar coordinates</h3>
<p>The area element is a thin <strong>triangle</strong> with its apex at the origin, not a rectangle. A wedge of angle <span class="mth"><i>dθ</i></span> at radius <span class="mth"><i>r</i></span> has area <span class="mth">½ <i>r</i> (<i>r</i> <i>dθ</i>)</span>, so</p>
<div class="eqb"><span class="mth"><i>A</i> <span class="op">=</span> <span class="frac"><span class="nm">1</span><span class="den">2</span></span> ∫<sub>α</sub><sup>β</sup> <i>r</i>² <i>dθ</i></span></div>
<p>and a step has a radial part <span class="mth"><i>dr</i></span> and a transverse part <span class="mth"><i>r</i> <i>dθ</i></span>, at right angles, so <span class="mth"><i>ds</i>² = <i>dr</i>² + <i>r</i>²<i>dθ</i>²</span> and</p>
<div class="eqb"><span class="mth"><i>L</i> <span class="op">=</span> ∫ √<span class="rad"><i>r</i>² <span class="op">+</span> (<i>dr</i>/<i>dθ</i>)²</span> <i>dθ</i></span></div>
<p>That same <span class="mth"><i>r</i></span> reappears as the polar area element <span class="mth"><i>r</i> <i>dr</i> <i>dθ</i></span> in the integration wing and as the polar Jacobian in the partial-derivatives wing. Three appearances of one geometric fact: <em>the grid spreads out as you move away from the pole</em>.</p>
<p>Named curves worth knowing: the <strong>rose</strong> <span class="mth"><i>r</i> = <i>a</i> cos <i>kθ</i></span> has <span class="mth"><i>k</i></span> petals for odd <span class="mth"><i>k</i></span> and <span class="mth">2<i>k</i></span> for even, because an odd <span class="mth"><i>k</i></span> retraces its petals on the second circuit; the <strong>cardioid</strong> <span class="mth"><i>r</i> = <i>a</i>(1 + cos θ)</span> encloses <span class="mth">3π<i>a</i>²/2</span> and has perimeter exactly <span class="mth">8<i>a</i></span>; the <strong>logarithmic spiral</strong> is self-similar, which is why a moth flying at a fixed angle to a light spirals into it.</p>

<h3 id="c6">Conic sections</h3>
<p>One definition, one number. A conic is the set of points whose distance to a <strong>focus</strong> is <span class="mth"><i>e</i></span> times the distance to a <strong>directrix</strong>. With the focus at the origin this is</p>
<div class="eqb"><span class="mth"><i>r</i> <span class="op">=</span> <span class="frac"><span class="nm"><i>ep</i></span><span class="den">1 <span class="op">+</span> <i>e</i> cos θ</span></span></span></div>
<p>and the eccentricity does everything: <span class="mth"><i>e</i> = 0</span> a circle, <span class="mth"><i>e</i> &lt; 1</span> an ellipse, <span class="mth"><i>e</i> = 1</span> a parabola, <span class="mth"><i>e</i> &gt; 1</span> a hyperbola. Drag <span class="mth"><i>e</i></span> slowly through 1 in the lab and watch the far end of the curve run away to infinity and return on the other side.</p>
<p>For the closed cases the standard-position data follow from the definition: <span class="mth"><i>c</i> = <i>ae</i></span>, and <span class="mth"><i>b</i>² = <i>a</i>² − <i>c</i>²</span> for the ellipse or <span class="mth"><i>b</i>² = <i>c</i>² − <i>a</i>²</span> for the hyperbola, whose asymptotes have slope <span class="mth">±<i>b</i>/<i>a</i></span>. The lab checks each identity numerically as you drag.</p>
<p class="note">Apollonius classified these around 200 BC by slicing a cone, with no algebra whatsoever, and the names are his: the eccentricity is the ratio of the cutting plane's slope to the cone's own. That the geometric classification and the focus–directrix one agree is a theorem, and it took two thousand years and Dandelin's spheres to prove cleanly.</p>
<p>The polar form is the one that matters in physics. Put the Sun at the focus and <em>every</em> orbit — bound, parabolic escape, hyperbolic flyby — is this single equation, with <span class="mth"><i>e</i></span> set by the energy. Kepler's first law is the statement that the inverse-square force produces exactly this family and no other.</p>

<h3 id="c7">Vector-valued functions</h3>
<p>A space curve is <span class="mth"><b>r</b>(<i>t</i>) = ⟨<i>x</i>(<i>t</i>), <i>y</i>(<i>t</i>), <i>z</i>(<i>t</i>)⟩</span>, and it is differentiated one component at a time. That is a theorem rather than a convention: the limit of a vector difference quotient exists exactly when each component's does. Everything follows — the product rules for both kinds of product,</p>
<div class="eqb"><span class="mth">(<b>u</b><span class="op">·</span><b>v</b>)′ <span class="op">=</span> <b>u</b>′<span class="op">·</span><b>v</b> <span class="op">+</span> <b>u</b><span class="op">·</span><b>v</b>′ , &nbsp;&nbsp; (<b>u</b><span class="op">×</span><b>v</b>)′ <span class="op">=</span> <b>u</b>′<span class="op">×</span><b>v</b> <span class="op">+</span> <b>u</b><span class="op">×</span><b>v</b>′</span></div>
<p>with the order preserved in the second because the cross product does not commute.</p>
<p class="note">One consequence is used constantly. If <span class="mth">|<b>u</b>|</span> is constant then <span class="mth"><b>u</b><span class="op">·</span><b>u</b></span> is constant, so differentiating gives <span class="mth"><b>u</b><span class="op">·</span><b>u</b>′ = 0</span>: <strong>a vector of constant length has a derivative perpendicular to itself</strong>. That is why constant-speed motion has acceleration entirely perpendicular to the velocity, why the unit tangent's derivative points across the curve, and why a satellite in a circular orbit accelerates hard while its speed never changes.</p>
<h4>Integrating a vector-valued function</h4>
<p>Integration is componentwise too, for the same reason:</p>
<div class="eqb"><span class="mth">∫ <b>r</b>(<i>t</i>) <i>dt</i> <span class="op">=</span> ⟨∫<i>x</i>(<i>t</i>)<i>dt</i>, ∫<i>y</i>(<i>t</i>)<i>dt</i>, ∫<i>z</i>(<i>t</i>)<i>dt</i>⟩ <span class="op">+</span> <b>C</b></span></div>
<p>with a constant <em>vector</em> of integration, which is exactly the three arbitrary constants three scalar integrations would produce. This is how a trajectory is recovered from a force: integrate the acceleration once for <span class="mth"><b>v</b>(<i>t</i>) = <b>v</b>₀ + ∫<b>a</b></span> and again for <span class="mth"><b>r</b>(<i>t</i>)</span>, with the two constants fixed by the initial velocity and position. The projectile stage does precisely that with <span class="mth"><b>a</b> = ⟨0, −<i>g</i>⟩</span>, and the result is the closed form it draws.</p>

<h3 id="c8">Curvature, torsion and the moving frame</h3>
<p>Three unit vectors, each built from the one before:</p>
<div class="eqb"><span class="mth">T̂ <span class="op">=</span> <span class="frac"><span class="nm"><b>r</b>′</span><span class="den">|<b>r</b>′|</span></span> , &nbsp;&nbsp; N̂ <span class="op">=</span> <span class="frac"><span class="nm">T̂′</span><span class="den">|T̂′|</span></span> , &nbsp;&nbsp; B̂ <span class="op">=</span> T̂ <span class="op">×</span> N̂</span></div>
<p><span class="mth">T̂</span> points along the motion. <span class="mth">N̂</span> points the way the curve is turning — always towards the concave side, and always perpendicular to <span class="mth">T̂</span> by the constant-length argument above. <span class="mth">B̂</span> completes a right-handed frame and is normal to the plane the curve momentarily lies in, the <strong>osculating plane</strong>.</p>
<p><strong>Curvature</strong> is the rate the direction turns per unit length, <span class="mth">κ = |<i>d</i>T̂/<i>ds</i>|</span>, and in a usable form</p>
<div class="eqb"><span class="mth">κ <span class="op">=</span> <span class="frac"><span class="nm">|<b>r</b>′ <span class="op">×</span> <b>r</b>″|</span><span class="den">|<b>r</b>′|³</span></span> , &nbsp;&nbsp; τ <span class="op">=</span> <span class="frac"><span class="nm">(<b>r</b>′ <span class="op">×</span> <b>r</b>″) <span class="op">·</span> <b>r</b>‴</span><span class="den">|<b>r</b>′ <span class="op">×</span> <b>r</b>″|²</span></span></span></div>
<p>The reciprocal <span class="mth">1/κ</span> is the radius of the <strong>osculating circle</strong>, the circle that hugs the curve best. <strong>Torsion</strong> <span class="mth">τ</span> is the rate the osculating plane itself twists: it is zero for every plane curve and nonzero exactly when the curve refuses to lie in any plane. Set the helix's pitch to zero in the lab and watch <span class="mth">τ</span> collapse while <span class="mth">κ</span> settles on <span class="mth">1/<i>a</i></span>.</p>
<p class="note">κ and τ determine a curve <em>completely</em>, up to a rigid motion: prescribe the two functions of arc length and there is exactly one curve, up to where you put it and which way it faces. That is the fundamental theorem of space curves, and it is the sense in which these two numbers <em>are</em> the shape.</p>

<h3 id="c9">Motion in space</h3>
<p>Velocity is <span class="mth"><b>v</b> = <b>r</b>′</span>, speed is <span class="mth">|<b>v</b>|</span>, acceleration is <span class="mth"><b>a</b> = <b>r</b>″</span>. The useful statement is that acceleration splits into exactly two parts and no more:</p>
<div class="eqb"><span class="mth"><b>a</b> <span class="op">=</span> <i>a</i><sub>T</sub> T̂ <span class="op">+</span> <i>a</i><sub>N</sub> N̂ , &nbsp;&nbsp; <i>a</i><sub>T</sub> <span class="op">=</span> <span class="frac"><span class="nm"><i>d</i>|<b>v</b>|</span><span class="den"><i>dt</i></span></span> <span class="op">=</span> <span class="frac"><span class="nm"><b>v</b><span class="op">·</span><b>a</b></span><span class="den">|<b>v</b>|</span></span> , &nbsp;&nbsp; <i>a</i><sub>N</sub> <span class="op">=</span> κ|<b>v</b>|²</span></div>
${stThm('Acceleration has exactly two components, and never a binormal one', {
  hyp:'<span class="mth"><b>r</b>(<i>t</i>)</span> is twice differentiable with <span class="mth"><b>v</b> = <b>r</b>′ ≠ <b>0</b></span>',
  then:'',
  eq: '<b>a</b> <span class="op">=</span> <span class="frac"><span class="nm"><i>d</i>|<b>v</b>|</span><span class="den"><i>dt</i></span></span> T̂ <span class="op">+</span> κ|<b>v</b>|² N̂ , &nbsp;&nbsp; <b>a</b><span class="op">·</span>B̂ <span class="op">=</span> 0',
  proof:`<p>Write the velocity as speed times direction, <span class="mth"><b>v</b> = |<b>v</b>| T̂</span>, and differentiate with the product rule:</p>
${stEq('<b>a</b> <span class="op">=</span> <span class="frac"><span class="nm"><i>d</i>|<b>v</b>|</span><span class="den"><i>dt</i></span></span> T̂ <span class="op">+</span> |<b>v</b>| <span class="frac"><span class="nm"><i>d</i>T̂</span><span class="den"><i>dt</i></span></span>')}
<p>The first term is already along <span class="mth">T̂</span>. For the second, use the chain rule to convert to arc length, <span class="mth"><i>d</i>T̂/<i>dt</i> = (<i>d</i>T̂/<i>ds</i>)(<i>ds</i>/<i>dt</i>) = (<i>d</i>T̂/<i>ds</i>)|<b>v</b>|</span>. By definition <span class="mth"><i>d</i>T̂/<i>ds</i> = κN̂</span>. So the second term is <span class="mth">κ|<b>v</b>|²N̂</span>.</p>
<p>That already proves the decomposition. The absence of a binormal component is now automatic, but it is worth seeing <em>why</em> <span class="mth"><i>d</i>T̂/<i>ds</i></span> could never have pointed along <span class="mth">B̂</span>: <span class="mth">T̂</span> is a <strong>unit</strong> vector, so <span class="mth">T̂·T̂ = 1</span>, and differentiating that constant gives</p>
${stEq('2 T̂ <span class="op">·</span> <span class="frac"><span class="nm"><i>d</i>T̂</span><span class="den"><i>ds</i></span></span> <span class="op">=</span> 0')}
<p>so <span class="mth"><i>d</i>T̂/<i>ds</i></span> is perpendicular to <span class="mth">T̂</span> — and <span class="mth">N̂</span> is <em>defined</em> as its direction. Since <span class="mth">B̂ = T̂ × N̂</span> is perpendicular to both, no part of <span class="mth"><b>a</b></span> can reach it.</p>
<p>Taking the magnitude of the decomposition, and using that <span class="mth">T̂</span> and <span class="mth">N̂</span> are orthogonal unit vectors, gives <span class="mth"><i>a</i><sub>T</sub>² + <i>a</i><sub>N</sub>² = |<b>a</b>|²</span> — the Pythagorean identity the lab's readout checks against the directly computed <span class="mth">|<b>a</b>|</span>.</p>`,
  note:'The constant-length argument is the reusable idea: any vector of fixed length has a derivative perpendicular to itself. It is why circular motion is centripetal, why a magnetic force does no work, and why the Frenet frame stays orthonormal.',
  see:'curves:1.3', seeLabel:'Acceleration splits in two, and only two' })}

<p>There is no component along <span class="mth">B̂</span>, ever — the acceleration always lies in the osculating plane, which is one honest answer to what that plane is for. The identity <span class="mth"><i>a</i><sub>T</sub>² + <i>a</i><sub>N</sub>² = |<b>a</b>|²</span> is what the lab's readout checks.</p>
<p>A car's accelerator and brake control <span class="mth"><i>a</i><sub>T</sub></span>; its steering wheel controls <span class="mth"><i>a</i><sub>N</sub></span>. They are different physical quantities that happen to share a name, and a great deal of confusion in elementary mechanics comes from not separating them. In projectile motion gravity never changes — what changes is how much of it is turning the path and how much is changing the speed, which is why the trajectory is most sharply curved at the apex.</p>
<h4>Kepler's second law</h4>
<p>If the force is <strong>central</strong> then <span class="mth"><b>r</b> <span class="op">×</span> <b>F</b> = 0</span>, so <span class="mth"><i>d</i>(<b>r</b> <span class="op">×</span> <b>v</b>)/<i>dt</i> = 0</span> and angular momentum is conserved. The areal rate is <span class="mth">½|<b>r</b> <span class="op">×</span> <b>v</b>|</span>, so it is constant too: <strong>equal areas in equal times</strong>. Kepler found this in 1609 from Tycho's data; Newton showed it holds for <em>any</em> central force, inverse-square or not. The lab obtains the position by solving Kepler's equation <span class="mth"><i>M</i> = <i>E</i> − <i>e</i> sin <i>E</i></span> numerically, so the motion is genuinely uniform in area rather than drawn to suggest it — and it measures the two wedges to prove the point.</p>
`;

