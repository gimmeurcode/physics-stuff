/* ============================================================================
   7 · THE LONG-FORM MATHEMATICS
   ============================================================================ */
const THEORY_VC_1 = `
<div class="toc">
  <a href="#t0">Using the lab</a>
  <a href="#t1">The operator</a><a href="#t2">Gradient</a><a href="#t3">Divergence</a>
  <a href="#t4">Curl</a><a href="#t5">The Jacobian</a>
  <a href="#t13">Line integrals</a><a href="#t14">Conservative fields</a><a href="#t15">Green's theorem</a>
  <a href="#t16">Surface integrals</a><a href="#t6">Stokes &amp; divergence</a>
  <a href="#t7">Identities</a><a href="#t9">Two dimensions</a><a href="#t10">Physics</a>
  <a href="#t11">Optimization</a><a href="#t12">Mechanics &amp; quantum</a><a href="#t8">Reading the pictures</a>
</div>

<h3 id="t0">Using the lab</h3>
<p>Everything on screen is computed live from whatever field is loaded, evaluated <strong>at the probe</strong> — the white dot. Nothing is pre-rendered.</p>
<ul>
  <li><strong>Start with Worked examples.</strong> Each button loads a field, sets the right view and layers, and states the outcome to look for. Groups run from the simplest possible fields up through AP physics and quantum mechanics.</li>
  <li><strong>Three views</strong> (top bar): <em>3D field</em> shows a block of space; <em>Height map</em> draws a scalar as terrain; <em>2D map</em> is the flat topographic view where the honest two-dimensional operators (scalar curl, 2D divergence) take over.</li>
  <li><strong>The probe</strong>: click anywhere — or grab and drag the dot — and every readout follows: f, ∇f, ∇·F, ∇×F, the directional derivative, the eigenvalue meter ∇²f/f, the Jacobian, and the symbolic derivation with your point substituted in.</li>
  <li><strong>Display &amp; layers</strong> switches what is drawn: arrows, field lines, flow, colour maps, level sets, and the curl field. Signed quantities always use the warm-positive / cool-negative scale; legends show the live numeric range.</li>
  <li><strong>Type anything</strong> into the field inputs — expressions in x, y, z and t. Mention t and the field animates (a Time control appears). The Physics builder instead <em>generates</em> the formula from charges, masses, wires, dipoles and antennas you place.</li>
  <li><strong>Instruments</strong>: the flux box and circulation loop evaluate the defining integrals numerically and compare them with the symbolic answer; the test particle integrates Newton's second law; the Optimization lab runs gradient descent, races, constraints and basin maps.</li>
</ul>

<h3 id="t1">One operator, three products</h3>
<p>Write the symbol <span class="mth">∇</span> ("del", or nabla) for the vector of partial-derivative operators:</p>
<div class="eqb"><span class="mth">∇ <span class="op">=</span> ( <span class="frac"><span class="nm">∂</span><span class="den">∂<i>x</i></span></span> , <span class="frac"><span class="nm">∂</span><span class="den">∂<i>y</i></span></span> , <span class="frac"><span class="nm">∂</span><span class="den">∂<i>z</i></span></span> )</span></div>
<p>This is not a vector of numbers — it is a vector of instructions, each waiting for a function to differentiate. But because it has the <em>shape</em> of a vector, exactly three products are available, and each one is a different operator:</p>
<ul>
  <li><strong>Scalar multiplication</strong> — <span class="mth">∇<i>f</i></span>, an operator on a scalar field returning a vector field. This is the <strong>gradient</strong>.</li>
  <li><strong>Dot product</strong> — <span class="mth">∇<span class="op">·</span><i>F</i></span>, on a vector field returning a scalar field. This is the <strong>divergence</strong>.</li>
  <li><strong>Cross product</strong> — <span class="mth">∇<span class="op">×</span><i>F</i></span>, on a vector field returning a vector field. This is the <strong>curl</strong>.</li>
</ul>
<p>There are no others, because there are no other products of a vector. That is the whole taxonomy. The notation is a mnemonic rather than a definition — each operator has its own honest definition below, and in every case the coordinate formula is a <em>theorem</em>, not the starting point.</p>

<h3 id="t2">Gradient — ∇f</h3>
<p>Fix a point and ask: if I step a small distance in the direction of a unit vector <span class="mth">û</span>, how fast does <span class="mth"><i>f</i></span> change? That rate is the <strong>directional derivative</strong>:</p>
<div class="eqb"><span class="mth"><i>D</i><sub>û</sub><i>f</i> <span class="op">=</span> lim<sub><i>h</i>→0</sub> <span class="frac"><span class="nm"><i>f</i>(<b>p</b> + <i>h</i>û) <span class="op">−</span> <i>f</i>(<b>p</b>)</span><span class="den"><i>h</i></span></span></span></div>
<p>Computing it for the three coordinate directions gives the three partial derivatives. If <span class="mth"><i>f</i></span> is differentiable, every other direction is a linear combination of those three, and</p>
<div class="eqb"><span class="mth"><i>D</i><sub>û</sub><i>f</i> <span class="op">=</span> ∇<i>f</i> <span class="op">·</span> û , &nbsp; where &nbsp; ∇<i>f</i> <span class="op">=</span> ( <span class="frac"><span class="nm">∂<i>f</i></span><span class="den">∂<i>x</i></span></span> , <span class="frac"><span class="nm">∂<i>f</i></span><span class="den">∂<i>y</i></span></span> , <span class="frac"><span class="nm">∂<i>f</i></span><span class="den">∂<i>z</i></span></span> )</span></div>
<p>Everything else about the gradient follows from that one identity, because <span class="mth">∇<i>f</i> <span class="op">·</span> û <span class="op">=</span> |∇<i>f</i>| cos θ</span>:</p>
<ul>
  <li><strong>Direction of steepest ascent.</strong> The dot product is largest when <span class="mth">θ = 0</span>, so <span class="mth">∇<i>f</i></span> points the way <span class="mth"><i>f</i></span> increases fastest.</li>
  <li><strong>Magnitude is that maximal rate.</strong> At <span class="mth">θ = 0</span> the value is <span class="mth">|∇<i>f</i>|</span>.</li>
  <li><strong>Perpendicular to level sets.</strong> Along a level set <span class="mth"><i>f</i></span> does not change, so <span class="mth"><i>D</i><sub>û</sub><i>f</i> = 0</span>, so <span class="mth">∇<i>f</i> <span class="op">·</span> û = 0</span>. Every direction tangent to the level set is orthogonal to the gradient. Turn on <strong>Level sets</strong> and <strong>Probe vectors</strong> together and you can watch this hold at every point.</li>
</ul>
<h4>Seeing the directional derivative</h4>
<p>Turn on the <strong>Directional derivative</strong> layer. It draws four things at the probe: the vector <span class="mth">∇<i>f</i></span>, your chosen direction <span class="mth">û</span>, the thick bar along <span class="mth">û</span> whose signed length <em>is</em> <span class="mth"><i>D</i><sub>û</sub><i>f</i></span>, and a thin drop line from the tip of <span class="mth">∇<i>f</i></span> meeting <span class="mth">û</span> at a right angle. That drop line is the whole content of the dot product: <strong>the directional derivative is the orthogonal projection of the gradient onto the direction you are asking about.</strong></p>
<p>Around them is a <strong>polar rose</strong>: for every direction in the plane, the curve is plotted at distance <span class="mth"><i>D</i><sub>û</sub><i>f</i></span> from the probe, drawn warm where the rate is positive and cool where it is negative. Because <span class="mth"><i>D</i><sub>û</sub><i>f</i> = |∇<i>f</i>| cos θ</span>, that curve is a pair of tangent circles — the polar graph of a cosine — with the two lobes meeting exactly where <span class="mth">û</span> is tangent to a level set. Those two zero directions are drawn as faint straight lines. Drag the <strong>Angle</strong> slider and watch the bar grow, shrink, vanish and reverse.</p>
<p>For a vector field the same diagram shows <span class="mth">(û<span class="op">·</span>∇)<i>F</i> = <i>J</i>û</span>, the full rate of change of <span class="mth"><i>F</i></span> along <span class="mth">û</span>, with the bar giving its component along <span class="mth">û</span> — the rate at which the field stretches in the direction you are moving. Averaging that stretching rate over all directions gives exactly one third of the divergence.</p>
<p class="note">A gradient is a <em>covector</em> in disguise: what is really defined is the linear map <span class="mth">û ↦ <i>D</i><sub>û</sub><i>f</i></span>, and turning that map into an arrow quietly uses the dot product. In Cartesian coordinates with an orthonormal basis the distinction is invisible, which is why it is safe to ignore here — but it is the reason gradients transform differently from ordinary vectors when you change to, say, spherical coordinates.</p>

<h3 id="t3">Divergence — ∇·F</h3>
<p>Think of <span class="mth"><i>F</i></span> as the velocity of a fluid. Surround a point with a closed surface <span class="mth"><i>S</i></span> enclosing volume <span class="mth"><i>V</i></span>. The <strong>flux</strong> through it — the net rate at which fluid leaves — is</p>
<div class="eqb"><span class="mth">Φ <span class="op">=</span> ∯<sub><i>S</i></sub> <i>F</i> <span class="op">·</span> n̂ <i>dS</i></span></div>
<p>where <span class="mth">n̂</span> is the outward unit normal, so <span class="mth"><i>F</i><span class="op">·</span>n̂</span> is the outward-pointing part of the flow. Flux is an extensive quantity: double the region and you roughly double it. To get a property <em>of the point</em>, divide by volume and shrink:</p>
<div class="eqb"><span class="mth">∇<span class="op">·</span><i>F</i> <span class="op">=</span> lim<sub><i>V</i>→0</sub> <span class="frac"><span class="nm">1</span><span class="den"><i>V</i></span></span> ∯<sub><i>S</i></sub> <i>F</i> <span class="op">·</span> n̂ <i>dS</i></span></div>
<p><strong>That limit is the definition.</strong> The <strong>Flux box</strong> panel evaluates the right-hand side numerically — it genuinely adds up <span class="mth"><i>F</i><span class="op">·</span>n̂ Δ<i>S</i></span> over the faces, never consulting a derivative — and prints it beside the symbolic answer so you can watch them agree.</p>
<h4>Where the coordinate formula comes from</h4>
<p>Take the box <span class="mth">[<i>x</i>, <i>x</i>+Δ<i>x</i>] × [<i>y</i>, <i>y</i>+Δ<i>y</i>] × [<i>z</i>, <i>z</i>+Δ<i>z</i>]</span>. On the two faces perpendicular to <span class="mth"><i>x</i></span> the outward normals are <span class="mth">+x̂</span> and <span class="mth">−x̂</span>, so those two faces contribute</p>
<div class="eqb"><span class="mth">[ <i>P</i>(<i>x</i>+Δ<i>x</i>, <i>y</i>, <i>z</i>) <span class="op">−</span> <i>P</i>(<i>x</i>, <i>y</i>, <i>z</i>) ] Δ<i>y</i> Δ<i>z</i> &nbsp;≈&nbsp; <span class="frac"><span class="nm">∂<i>P</i></span><span class="den">∂<i>x</i></span></span> Δ<i>x</i> Δ<i>y</i> Δ<i>z</i></span></div>
<p>The other two pairs give the same thing with <span class="mth"><i>Q</i>, <i>y</i></span> and <span class="mth"><i>R</i>, <i>z</i></span>. Adding all six faces and dividing by the volume <span class="mth">Δ<i>x</i>Δ<i>y</i>Δ<i>z</i></span>, then letting it shrink:</p>
<div class="eqb"><span class="mth">∇<span class="op">·</span><i>F</i> <span class="op">=</span> <span class="frac"><span class="nm">∂<i>P</i></span><span class="den">∂<i>x</i></span></span> <span class="op">+</span> <span class="frac"><span class="nm">∂<i>Q</i></span><span class="den">∂<i>y</i></span></span> <span class="op">+</span> <span class="frac"><span class="nm">∂<i>R</i></span><span class="den">∂<i>z</i></span></span></span></div>
<p>Each term measures spreading along its own axis, and they simply add. Positive divergence means the point is a <strong>source</strong>, negative a <strong>sink</strong>, and zero means whatever flows in flows back out — an incompressible flow.</p>
<p class="note">Load <strong>Inverse-square (point charge)</strong> and look carefully. Every arrow points away from the origin, so it <em>looks</em> like a source everywhere — yet the divergence is exactly zero at every point except the origin. The spreading of the directions is precisely cancelled by the <span class="mth">1/<i>r</i>²</span> decay in magnitude. All of the source is concentrated at the single point where the field is undefined. This is Gauss's law: the flux through <em>any</em> surface enclosing the origin is the same non-zero constant, even though the divergence vanishes everywhere the field is defined.</p>

<h3 id="t4">Curl — ∇×F</h3>
<p>Divergence asked about flow <em>through</em> a surface. Curl asks about flow <em>around</em> a loop. The <strong>circulation</strong> of <span class="mth"><i>F</i></span> around a closed curve <span class="mth"><i>C</i></span> is</p>
<div class="eqb"><span class="mth">Γ <span class="op">=</span> ∮<sub><i>C</i></sub> <i>F</i> <span class="op">·</span> <i>d</i><b>r</b> <span class="op">=</span> ∮<sub><i>C</i></sub> <i>F</i> <span class="op">·</span> T̂ <i>ds</i></span></div>
<p>with <span class="mth">T̂</span> the unit tangent. This measures how much the field runs <em>along</em> the loop rather than across it. Again divide by the enclosed area and shrink — but there is a new wrinkle. A loop in three dimensions has an <strong>orientation</strong>: it lies in some plane, described by its unit normal <span class="mth">n̂</span> (with the loop traversed counter-clockwise seen from the tip of <span class="mth">n̂</span> — the right-hand rule). So the limit defines one number <em>per direction</em>:</p>
<div class="eqb"><span class="mth">(∇<span class="op">×</span><i>F</i>) <span class="op">·</span> n̂ <span class="op">=</span> lim<sub><i>A</i>→0</sub> <span class="frac"><span class="nm">1</span><span class="den"><i>A</i></span></span> ∮<sub><i>C</i></sub> <i>F</i> <span class="op">·</span> <i>d</i><b>r</b></span></div>
<p>Remarkably, that whole family of numbers — one for every possible orientation — is captured by a single vector. The map <span class="mth">n̂ ↦ (circulation per unit area)</span> turns out to be linear, and every linear map from directions to numbers is a dot product with some fixed vector. That vector is the curl. Its <strong>direction</strong> is the axis about which the local rotation is fastest, and its <strong>magnitude</strong> is twice that fastest rotation rate. Press <strong>Sweep n̂ through every direction</strong> in the circulation panel to see the cosine falloff, maximal at <span class="mth">n̂ ∥ ∇<span class="op">×</span><i>F</i></span> and exactly zero at <span class="mth">n̂ ⊥ ∇<span class="op">×</span><i>F</i></span>.</p>
<h4>Where the coordinate formula comes from</h4>
<p>Take a small rectangle in the <span class="mth"><i>xy</i></span>-plane with corner <span class="mth">(<i>x</i>, <i>y</i>)</span> and sides <span class="mth">Δ<i>x</i>, Δ<i>y</i></span>, traversed counter-clockwise so <span class="mth">n̂ = ẑ</span>. Walking the four edges and keeping only the tangential component on each:</p>
<div class="eqb"><span class="mth">Γ ≈ <i>P</i>(<i>x</i>,<i>y</i>)Δ<i>x</i> <span class="op">+</span> <i>Q</i>(<i>x</i>+Δ<i>x</i>,<i>y</i>)Δ<i>y</i> <span class="op">−</span> <i>P</i>(<i>x</i>,<i>y</i>+Δ<i>y</i>)Δ<i>x</i> <span class="op">−</span> <i>Q</i>(<i>x</i>,<i>y</i>)Δ<i>y</i></span></div>
<p>Group the two <span class="mth"><i>P</i></span> terms and the two <span class="mth"><i>Q</i></span> terms into difference quotients and divide by the area <span class="mth">Δ<i>x</i>Δ<i>y</i></span>:</p>
<div class="eqb"><span class="mth">(∇<span class="op">×</span><i>F</i>)<sub><i>z</i></sub> <span class="op">=</span> <span class="frac"><span class="nm">∂<i>Q</i></span><span class="den">∂<i>x</i></span></span> <span class="op">−</span> <span class="frac"><span class="nm">∂<i>P</i></span><span class="den">∂<i>y</i></span></span></span></div>
<p>Cycling <span class="mth"><i>x</i>→<i>y</i>→<i>z</i>→<i>x</i></span> gives the other two components:</p>
<div class="eqb"><span class="mth">∇<span class="op">×</span><i>F</i> <span class="op">=</span> ( <span class="frac"><span class="nm">∂<i>R</i></span><span class="den">∂<i>y</i></span></span><span class="op">−</span><span class="frac"><span class="nm">∂<i>Q</i></span><span class="den">∂<i>z</i></span></span> , <span class="frac"><span class="nm">∂<i>P</i></span><span class="den">∂<i>z</i></span></span><span class="op">−</span><span class="frac"><span class="nm">∂<i>R</i></span><span class="den">∂<i>x</i></span></span> , <span class="frac"><span class="nm">∂<i>Q</i></span><span class="den">∂<i>x</i></span></span><span class="op">−</span><span class="frac"><span class="nm">∂<i>P</i></span><span class="den">∂<i>y</i></span></span> )</span></div>
<p>which is the determinant mnemonic — expand along the first row and you get exactly these components:</p>
<div class="eqb"><span class="mth">∇<span class="op">×</span><i>F</i> <span class="op">=</span> det <span style="display:inline-block;border-left:1px solid;border-right:1px solid;padding:2px 7px;vertical-align:middle"><span style="display:block">x̂ &nbsp; ŷ &nbsp; ẑ</span><span style="display:block">∂<sub><i>x</i></sub> &nbsp; ∂<sub><i>y</i></sub> &nbsp; ∂<sub><i>z</i></sub></span><span style="display:block"><i>P</i> &nbsp; <i>Q</i> &nbsp; <i>R</i></span></span></span></div>
<h4>Why the factor of two, and why "spin" is the wrong word</h4>
<p>For a body rotating rigidly at angular velocity <span class="mth">ω</span>, the velocity field is <span class="mth"><i>F</i> = ω × <b>r</b></span>, and computing the curl gives <span class="mth">∇<span class="op">×</span><i>F</i> = 2ω</span>. So the honest statement is <span class="mth">ω = ½ ∇<span class="op">×</span><i>F</i></span> — the paddle wheel in this tool turns at exactly that rate, not at the curl itself.</p>
<p class="note">Load <strong>Simple shear</strong>, <span class="mth"><i>F</i> = (<i>y</i>, 0, 0)</span>. Nothing appears to rotate — every arrow points along <span class="mth">x̂</span> — yet the curl is <span class="mth">(0, 0, −1)</span>. Put the paddle wheel in it: the arrows above the axis push right harder than the arrows below push left, so it turns. Curl detects <em>local</em> rotation of fluid elements, which is not the same as the flow going around in circles. The <strong>Irrotational vortex</strong> preset is the exact mirror image: the flow visibly circles the axis, yet the curl is zero everywhere off it.</p>

<h3 id="t5">The Jacobian — all three at once</h3>
<p>Near a point, a differentiable vector field is well approximated by a linear map: the <strong>Jacobian</strong> matrix of all nine first partials.</p>
<div class="eqb"><span class="mth"><i>J</i><sub><i>ij</i></sub> <span class="op">=</span> <span class="frac"><span class="nm">∂<i>F</i><sub><i>i</i></sub></span><span class="den">∂<i>x</i><sub><i>j</i></sub></span></span></span></div>
<p>Every matrix splits uniquely into a symmetric and an antisymmetric part, <span class="mth"><i>J</i> = <i>S</i> + <i>A</i></span> with <span class="mth"><i>S</i> = ½(<i>J</i> + <i>J</i><sup>T</sup>)</span> and <span class="mth"><i>A</i> = ½(<i>J</i> − <i>J</i><sup>T</sup>)</span>. Those two halves are precisely our two operators:</p>
<ul>
  <li><span class="mth"><i>S</i></span> is the <strong>strain</strong> — stretching and shearing without rotation. Its trace is <span class="mth"><i>J</i><sub>11</sub>+<i>J</i><sub>22</sub>+<i>J</i><sub>33</sub></span>, which is the <strong>divergence</strong>. Its off-diagonal entries are pure shape change.</li>
  <li><span class="mth"><i>A</i></span> is an antisymmetric 3×3 matrix, and every such matrix acts as a cross product: <span class="mth"><i>A</i><b>v</b> = ω × <b>v</b></span> for a unique <span class="mth">ω</span>, namely <span class="mth">ω = ½∇<span class="op">×</span><i>F</i></span>. It is the <strong>rotation</strong>.</li>
</ul>
<p>So divergence and curl are not two unrelated inventions. They are the trace and the antisymmetric part of one object — the derivative of the field. What is left over, the traceless symmetric part, is shear, which neither operator sees. The <strong>Simple shear</strong> preset is exactly a field whose Jacobian is half shear and half rotation; open the two matrices in the probe panel and read the halves off directly.</p>
<p>For a <em>scalar</em> field the analogous second-order object is the <strong>Hessian</strong> <span class="mth"><i>H</i><sub><i>ij</i></sub> = ∂²<i>f</i>/∂<i>x</i><sub><i>i</i></sub>∂<i>x</i><sub><i>j</i></sub></span>, the Jacobian of <span class="mth">∇<i>f</i></span>. It is symmetric whenever the second partials are continuous — which is exactly why the curl of a gradient vanishes.</p>

`;
