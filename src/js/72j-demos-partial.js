const PARTIAL_GROUPS = [
{ g:'Functions of several variables', items:[
  {n:'Surfaces, contours and traces', ex:'three ways to look at f(x, y)', stage:'mvSurface',
   opts:{key:'ripple', view:'contour'},
   out:'The contour map reads like an Ordnance Survey sheet: contours close together mean steep ground. Switch to traces and each one is an ordinary single-variable curve, which is exactly where partial derivatives come from.',
   note:'A function of two variables is a surface over the plane, and a flat page can show it honestly in only two ways — as level curves, or as slices. The domain matters as much as the rule: √(x²+y²) is defined everywhere and yet has no tangent plane at the origin.'},
  {n:'Limits: why checking lines is never enough', ex:'x²y/(x⁴+y²) is zero on every line and ½ on a parabola', stage:'mvLimit',
   opts:{key:'square'},
   out:'Every straight approach gives 0, so a line test would wrongly pass this function. Along y = x² the value is ½ at every point. One curved path is enough to destroy the limit.',
   note:'In one variable there are two ways to approach a point; in two there are infinitely many, and a limit exists only if <i>every</i> one agrees. The decisive test is polar: if |f − L| ≤ g(r) with g → 0 <b>independently of θ</b>, the limit is L. The right-hand plot measures exactly that θ-spread as the radius shrinks.'},
  {n:'Partial derivatives are trace slopes', ex:'freeze one variable and differentiate', stage:'mvPartial',
   opts:{key:'ripple'},
   out:'The straight lines drawn on the two trace plots are the tangents whose slopes the readout prints. Nothing new is needed: one-variable calculus applies to a trace unchanged.',
   note:'Partial differentiation is ordinary differentiation with the other variable frozen into a constant. What is new is that there are now two of them — and, later, that they are not the whole story: a function can have both partials at a point and still fail to be differentiable there.'},
  {n:"Higher-order partials and Clairaut's theorem", ex:'f<sub>xy</sub> = f<sub>y</sub>x, along two different chains of algebra', stage:'mvPartial',
   opts:{key:'wavehill', which:'both'},
   out:'The two mixed partials are differentiated symbolically along different routes and the readout prints |f<sub>xy</sub> − f<sub>y</sub>x| as zero. Both derivations are shown in full.',
   note:'This says the order in which you take two different limits does not matter — which is not obvious, and there are (contrived) functions where it fails. The mixed partial measures the <b>twist</b> of the surface: how fast the slope in x changes as you move in y. It is zero exactly when the function separates as g(x) + h(y), and it is the off-diagonal Hessian entry that decides saddles.'}
]},
{ g:'Linear approximation and the chain rule', items:[
  {n:'The tangent plane, and what differentiability means', ex:'shrink the window and watch the surface flatten', stage:'mvTangent',
   opts:{key:'gauss2'},
   out:'The worst error over a circle of radius h falls as h² — halve h and the readout\'s ratio converges on 4. That convergence <i>is</i> differentiability.',
   note:'A tangent line in one variable is the linear function agreeing to first order. In two variables the same definition is a real extra condition, because it must hold in <i>every</i> direction at once, and having both partials only pins down two of them. Try the cone √(x²+y²) at the origin and watch the ratio refuse to settle.'},
  {n:'The differential and error propagation', ex:'dz = f<sub>x</sub> dx + f<sub>y</sub> dy', stage:'mvTangent',
   opts:{key:'ripple', h:0.4},
   out:'The differential predicts the change to first order, and the readout differences it against the true Δf. The gap is second order — which is why it is safe to ignore for small enough steps and dangerous otherwise.',
   note:'The differential is not an infinitesimal quantity; it is the <b>linear map</b> that the tangent plane is the graph of. This is how error propagation works in every laboratory: uncertainties in the inputs contribute |f<sub>x</sub>|δx + |f<sub>y</sub>|δy to the output, and that is the whole of the standard propagation formula.'},
  {n:'The chain rule is a dot product', ex:'df/dt = ∇f · r′(t)', stage:'mvChain',
   opts:{key:'ripple', pathKey:'circle'},
   out:'The panel computes df/dt two ways — by the chain rule, and by differencing the composite f(x(t), y(t)) as a single function of t — and prints the gap as zero.',
   note:'A tree diagram is bookkeeping for "multiply along each branch, add across the branches". Written as <b>df/dt = ∇f · r′</b> it becomes obvious that the chain rule and the directional derivative are the same statement. In matrix form it is <b>J(g∘f) = J(g)·J(f)</b> — the version the Jacobian stage draws.'},
  {n:'Implicit differentiation without solving', ex:'dy/dx = −F<sub>x</sub>/F<sub>y</sub> on F(x, y) = 0', stage:'mvChain',
   opts:{mode:'implicit'},
   out:'The dashed tangent is perpendicular to ∇F at every point — which is the entire geometric content. The folium x³+y³ = 3xy has no elementary y(x) at all, and its slope is still one division away.',
   note:'Differentiate F(x, y(x)) = 0 with the chain rule and you get F<sub>x</sub> + F<sub>y</sub>·y′ = 0 in one line. The implicit function theorem is the fine print: y really is a function of x near the point provided <b>F<sub>y</sub> ≠ 0</b> — exactly when the formula does not divide by zero. Where F<sub>y</sub> vanishes the tangent is vertical and no function y(x) can describe the curve there.'}
]},
{ g:'Gradients, extrema and constraints', items:[
  {n:'The gradient and every direction at once', ex:'D<sub>û</sub> f = ∇f · û = |∇f| cos θ', stage:'mvField',
   opts:{key:'wavehill'},
   out:'Click anywhere on the map. The thick bar is the directional derivative drawn as a length, and the rose is its value in every direction — a pair of tangent circles, because a cosine in polar form always is.',
   note:'Three facts fall out of one dot product and are all visible: the maximum rate is |∇f| along ∇f, the minimum is −|∇f| the other way, and the rate is <b>exactly zero</b> along the level curve. That last one is why a gradient is always perpendicular to the level set through its point, and why every steepest-descent algorithm ever written works.'},
  {n:'Critical points, found rather than given', ex:'∇f = 0, then the discriminant decides', stage:'mvCrit',
   opts:{key:'wavehill'},
   out:'Newton\'s method is launched from a grid onto the system f<sub>x</sub> = f<sub>y</sub> = 0 and duplicates are merged, so switching the function genuinely re-solves. The blue and green curves are f<sub>x</sub> = 0 and f<sub>y</sub> = 0 — the critical points are exactly where they cross.',
   note:'<b>D = f<sub>xx</sub> f<sub>yy</sub> − f<sub>xy</sub>²</b> is the determinant of the Hessian, so it is the product of the two eigenvalues. D &lt; 0 means opposite signs — up one way, down the other — a <b>saddle</b>. D &gt; 0 means they agree and f<sub>xx</sub> says which. The eigenvectors are drawn: for a saddle they are literally the uphill and downhill axes.'},
  {n:'When the test says nothing', ex:'the monkey saddle, D = 0', stage:'mvCrit',
   opts:{key:'monkey'},
   out:'x³ − 3xy² has exactly one critical point and the discriminant is zero there. The test is not being coy — the quadratic approximation is degenerate and the answer lives in the cubic terms.',
   note:'Three descending valleys and three ascending ridges meet at the origin: room for two legs and a tail, which is where the name comes from. It is worth meeting once, because it shows that the second-derivative test is a <i>sufficient</i> condition and not a decision procedure.'},
  {n:'Lagrange multipliers as tangency', ex:'∇f = λ∇g, and what λ is worth', stage:'mvLagr',
   opts:{prob:'circle'},
   out:'Walk the constraint and watch f rise and fall. At an extreme the constraint curve is tangent to a level curve of f — because if it crossed one you could step either way and change f. The solutions are found by root-finding on ∇f × ∇g.',
   note:'λ is not a nuisance variable. Relax the constraint from g = 0 to g = ε and the optimal value shifts by −λε to first order: it is the marginal value of another unit of the scarce resource. In mechanics it is the constraint force — the tension in the string that keeps the bead on the wire; in thermodynamics the multipliers of the maximum-entropy problem come out as temperature and chemical potential.'},
  {n:'The Jacobian — the derivative of a map', ex:'a matrix of partials, and the area it scales', stage:'mvJac',
   opts:{key:'polar'},
   out:'The little square is drawn, mapped corner by corner, and its image measured by the shoelace formula. Shrink the cell and the measured ratio converges on |det J| — that convergence is the definition of the derivative.',
   note:'The columns of J are the tangents to the two grid lines, so the determinant is the area of the parallelogram they span — a cross product in disguise, and the same object that appears as |r_u × r_v| in a surface integral. For the polar map the determinant is exactly <b>r</b>, which is the whole reason the polar area element is r dr dθ.'}
]}];

