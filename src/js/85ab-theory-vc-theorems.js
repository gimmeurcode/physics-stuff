const THEORY_VC_2 = `<h3 id="t13">Line integrals</h3>
<p>Two different objects share the name, and the difference is worth being precise about.</p>
<p>A <strong>scalar line integral</strong> adds <span class="mth"><i>f</i></span> up along a curve, weighted by arc length:</p>
<div class="eqb"><span class="mth">∫<sub><i>C</i></sub> <i>f</i> <i>ds</i> <span class="op">=</span> ∫<sub><i>a</i></sub><sup><i>b</i></sup> <i>f</i>(<b>r</b>(<i>t</i>)) |<b>r</b>′(<i>t</i>)| <i>dt</i></span></div>
<p>The picture is a curtain: erect a wall over <span class="mth"><i>C</i></span> whose height at each point is <span class="mth"><i>f</i></span>, and the integral is its area. Because <span class="mth"><i>ds</i></span> is a length it carries no direction — reversing the curve does not change the answer — and the total mass of a wire of variable density is exactly this integral.</p>
<p>A <strong>vector line integral</strong> adds up only the component of <span class="mth"><i>F</i></span> running <em>along</em> the curve:</p>
<div class="eqb"><span class="mth">∫<sub><i>C</i></sub> <i>F</i> <span class="op">·</span> <i>d</i><b>r</b> <span class="op">=</span> ∫<sub><i>a</i></sub><sup><i>b</i></sup> <i>F</i>(<b>r</b>(<i>t</i>)) <span class="op">·</span> <b>r</b>′(<i>t</i>) <i>dt</i> <span class="op">=</span> ∫<sub><i>C</i></sub> <i>P dx</i> <span class="op">+</span> <i>Q dy</i> <span class="op">+</span> <i>R dz</i></span></div>
<p>This is <strong>work</strong>, and it is orientation-sensitive: run the curve backwards and the answer changes sign, because <span class="mth"><i>d</i><b>r</b></span> does. That is not a technicality — work done <em>against</em> a force is the negative of work done <em>by</em> it, and the sign is the whole point.</p>
<p>In the plane there is a third form, the <strong>flux across a curve</strong>, obtained by using the outward normal instead of the tangent:</p>
<div class="eqb"><span class="mth">∮<sub><i>C</i></sub> <i>F</i> <span class="op">·</span> n̂ <i>ds</i> <span class="op">=</span> ∮<sub><i>C</i></sub> <i>P dy</i> <span class="op">−</span> <i>Q dx</i></span></div>
<p class="note">A curve with corners has no <span class="mth"><b>r</b>′</span> there, so it must be integrated side by side. The lab does exactly that for its square path — integrating blind across a corner is the commonest way to get a wrong answer here.</p>

<h3 id="t14">Conservative fields</h3>
<p>On a <strong>simply connected</strong> domain, four statements are equivalent:</p>
<ul>
  <li><span class="mth"><i>F</i> = ∇<i>f</i></span> for some scalar <span class="mth"><i>f</i></span> (a <em>potential</em>).</li>
  <li><span class="mth">∮<i>F</i><span class="op">·</span><i>d</i><b>r</b> = 0</span> around every closed curve.</li>
  <li>The work between two points is the same along every path.</li>
  <li><span class="mth">∇<span class="op">×</span><i>F</i> = 0</span> — in the plane, <span class="mth">∂<i>P</i>/∂<i>y</i> = ∂<i>Q</i>/∂<i>x</i></span>.</li>
</ul>
<p>The last is the cheap test, and it works because mixed partials commute: <span class="mth">∂²<i>f</i>/∂<i>y</i>∂<i>x</i> = ∂²<i>f</i>/∂<i>x</i>∂<i>y</i></span>. Once <span class="mth"><i>f</i></span> exists the <strong>Fundamental Theorem for line integrals</strong> takes over:</p>
<div class="eqb"><span class="mth">∫<sub><i>C</i></sub> ∇<i>f</i> <span class="op">·</span> <i>d</i><b>r</b> <span class="op">=</span> <i>f</i>(end) <span class="op">−</span> <i>f</i>(start)</span></div>
<p>which is the one-variable Fundamental Theorem with the interval replaced by a curve. It is why potential energy is a useful idea at all, and why "the work done by gravity" can be quoted without asking which route you took.</p>
<p>To <em>recover</em> the potential, integrate along a staircase: <span class="mth">∫<i>P dx</i></span> along a horizontal leg, then <span class="mth">∫<i>Q dy</i></span> along a vertical one. Doing it in the other order must give the same answer, and the gap between the two routes is exactly the circulation around the rectangle they bound — which is why the two agree precisely when the cross-partial test passes.</p>
<p class="note"><strong>The catch.</strong> The cross-partial test detects only a <em>local</em> obstruction. The vortex field <span class="mth">⟨−<i>y</i>, <i>x</i>⟩/(<i>x</i>²+<i>y</i>²)</span> has zero curl at every point of its domain, and yet circulates <span class="mth">2π</span> around the origin — because the loop cannot be shrunk to a point without leaving the domain. It is locally a gradient (the potential is the polar angle) but not globally, since the angle increases by <span class="mth">2π</span> every time round. Winding numbers, branch cuts and the Aharonov–Bohm phase all live in that gap, and the "simply connected" clause is doing real work.</p>

<h3 id="t15">Green's theorem</h3>
<div class="eqb"><span class="mth">∮<sub><i>C</i></sub> <i>P dx</i> <span class="op">+</span> <i>Q dy</i> <span class="op">=</span> ∬<sub><i>R</i></sub> <span style="display:inline-block">(<span class="frac"><span class="nm">∂<i>Q</i></span><span class="den">∂<i>x</i></span></span> <span class="op">−</span> <span class="frac"><span class="nm">∂<i>P</i></span><span class="den">∂<i>y</i></span></span>)</span> <i>dA</i></span></div>
<p>The circulation around the boundary equals the total microscopic rotation inside. The proof is cancellation: tile the region with little loops, and every interior edge is traversed twice in opposite directions and cancels, leaving only the outer boundary. The lab draws those loops, because they are the argument rather than an illustration of it.</p>
<p>Applying the same theorem to the rotated field <span class="mth">⟨−<i>Q</i>, <i>P</i>⟩</span> gives the <strong>flux form</strong> — the two-dimensional divergence theorem:</p>
<div class="eqb"><span class="mth">∮<sub><i>C</i></sub> <i>F</i> <span class="op">·</span> n̂ <i>ds</i> <span class="op">=</span> ∬<sub><i>R</i></sub> (∇<span class="op">·</span><i>F</i>) <i>dA</i></span></div>
<p>One theorem, two costumes — and the common ancestor of both Stokes' theorem and the divergence theorem, which is exactly why those two look so alike.</p>
<p>Choosing <span class="mth"><i>P</i> = −<i>y</i>/2, <i>Q</i> = <i>x</i>/2</span> makes the integrand 1 and returns the <strong>area</strong>:</p>
<div class="eqb"><span class="mth"><i>A</i> <span class="op">=</span> <span class="frac"><span class="nm">1</span><span class="den">2</span></span> ∮<sub><i>C</i></sub> (<i>x dy</i> <span class="op">−</span> <i>y dx</i>)</span></div>
<p>A <em>planimeter</em> is a mechanical instrument that evaluates this by being wheeled once around the outline of a shape — an area measured without ever entering the region. The same integral evaluated on the vertices of a polygon is the shoelace formula every mapping package uses.</p>
<p>Orientation matters: the theorem holds for the <strong>positive</strong> orientation, with the region on your left. Reverse it and both sides flip together — which is consistent, but a trap if you only reverse one. For a region with holes the inner boundaries run the other way, again so that the region stays on your left.</p>

${stThm("Green's theorem", {
  hyp:'<span class="mth"><i>C</i></span> is a piecewise-smooth, simple, closed curve, positively oriented, bounding a region <span class="mth"><i>R</i></span>; and <span class="mth"><i>P</i>, <i>Q</i></span> have continuous first partials on an open set containing <span class="mth"><i>R</i></span>',
  then:'',
  eq:'∮<sub><i>C</i></sub> <i>P dx</i> <span class="op">+</span> <i>Q dy</i> <span class="op">=</span> ∬<sub><i>R</i></sub> <span style="display:inline-block">(<span class="frac"><span class="nm">∂<i>Q</i></span><span class="den">∂<i>x</i></span></span> <span class="op">−</span> <span class="frac"><span class="nm">∂<i>P</i></span><span class="den">∂<i>y</i></span></span>)</span> <i>dA</i>',
  proof:`<p>Prove it for a region that is simultaneously Type I and Type II; the general case follows by cutting into such pieces, and the cuts cancel because each is traversed once in each direction.</p>
<p><em>The <span class="mth"><i>P</i></span> half.</em> Write <span class="mth"><i>R</i></span> as Type I: <span class="mth"><i>a</i> ≤ <i>x</i> ≤ <i>b</i></span>, <span class="mth"><i>g</i>₁(<i>x</i>) ≤ <i>y</i> ≤ <i>g</i>₂(<i>x</i>)</span>. Then by Fubini and the Fundamental Theorem applied in <span class="mth"><i>y</i></span>,</p>
${stEq('∬<sub><i>R</i></sub> <span class="frac"><span class="nm">∂<i>P</i></span><span class="den">∂<i>y</i></span></span> <i>dA</i> <span class="op">=</span> ∫<sub><i>a</i></sub><sup><i>b</i></sup> [ <i>P</i>(<i>x</i>, <i>g</i>₂(<i>x</i>)) <span class="op">−</span> <i>P</i>(<i>x</i>, <i>g</i>₁(<i>x</i>)) ] <i>dx</i>')}
<p>Now compute <span class="mth">∮<sub><i>C</i></sub> <i>P dx</i></span> directly. The boundary splits into the lower curve <span class="mth"><i>y</i> = <i>g</i>₁(<i>x</i>)</span> traversed left to right, the upper curve <span class="mth"><i>y</i> = <i>g</i>₂(<i>x</i>)</span> traversed right to left, and possibly two vertical segments on which <span class="mth"><i>dx</i> = 0</span> and which therefore contribute nothing. So</p>
${stEq('∮<sub><i>C</i></sub> <i>P dx</i> <span class="op">=</span> ∫<sub><i>a</i></sub><sup><i>b</i></sup> <i>P</i>(<i>x</i>, <i>g</i>₁) <i>dx</i> <span class="op">−</span> ∫<sub><i>a</i></sub><sup><i>b</i></sup> <i>P</i>(<i>x</i>, <i>g</i>₂) <i>dx</i>')}
<p>Comparing the two displays, <span class="mth">∮ <i>P dx</i> = −∬ ∂<i>P</i>/∂<i>y</i> <i>dA</i></span>.</p>
<p><em>The <span class="mth"><i>Q</i></span> half</em> is the same argument with the roles of the axes exchanged, using the Type II description, and gives <span class="mth">∮ <i>Q dy</i> = +∬ ∂<i>Q</i>/∂<i>x</i> <i>dA</i></span>. The asymmetry of sign comes entirely from the orientation: going positively round, the <span class="mth"><i>x</i></span>-limits are traversed in the opposite sense to the <span class="mth"><i>y</i></span>-limits.</p>
<p>Adding the two halves is the theorem.</p>`,
  note:'"Simple" is load-bearing: the curve must not cross itself. So is the requirement that P and Q be differentiable on the whole of R — the vortex field −y/r², x/r² satisfies Q_x = P_y everywhere it is defined, yet has circulation 2π around the origin. The hole is not a technicality; it is the reason cohomology exists.',
  see:'vector:1.0', seeLabel:'Circulation form, both sides measured independently' })}

${stCor('Area from the boundary alone', {
  hyp:"the hypotheses of Green's theorem",
  then:'the area of <span class="mth"><i>R</i></span> is computable from a boundary integral:',
  eq:'<i>A</i> <span class="op">=</span> <span class="frac"><span class="nm">1</span><span class="den">2</span></span> ∮<sub><i>C</i></sub> (<i>x dy</i> <span class="op">−</span> <i>y dx</i>)',
  proof:`<p>Take <span class="mth"><i>P</i> = −<i>y</i>/2</span> and <span class="mth"><i>Q</i> = <i>x</i>/2</span>. Then</p>
${stEq('<span class="frac"><span class="nm">∂<i>Q</i></span><span class="den">∂<i>x</i></span></span> <span class="op">−</span> <span class="frac"><span class="nm">∂<i>P</i></span><span class="den">∂<i>y</i></span></span> <span class="op">=</span> ½ <span class="op">−</span> (<span class="op">−</span>½) <span class="op">=</span> 1')}
<p>so the double integral is <span class="mth">∬<sub><i>R</i></sub> 1 <i>dA</i></span>, which is the area by definition.</p>
<p>Evaluated on the vertices of a polygon this is the shoelace formula, and its <em>sign</em> is the orientation — which is why the lab's region tool can hand the same number to Green's theorem without asking the user which way round they drew the loop.</p>`,
  note:'A planimeter is this integral made of brass: wheel it once round an outline and read the area, without the instrument ever entering the region.',
  see:'vector:1.2', seeLabel:'The planimeter: area from the boundary alone' })}

<h3 id="t16">Surface integrals</h3>
<p>A parametrised surface is a map <span class="mth"><b>r</b>(<i>u</i>,<i>v</i>)</span> from a rectangle into space. The two tangent vectors <span class="mth"><b>r</b><sub><i>u</i></sub></span> and <span class="mth"><b>r</b><sub><i>v</i></sub></span> span the tangent plane, and the little patch they span is a parallelogram of area</p>
<div class="eqb"><span class="mth"><i>dS</i> <span class="op">=</span> |<b>r</b><sub><i>u</i></sub> <span class="op">×</span> <b>r</b><sub><i>v</i></sub>| <i>du dv</i></span></div>
<p>That is the Jacobian of the integration wing, one dimension up and one dimension out: the same cross-product-as-area-scale, now measuring how a flat parameter rectangle is stretched onto a curved surface. For a graph <span class="mth"><i>z</i> = <i>g</i>(<i>x</i>,<i>y</i>)</span> it reduces to <span class="mth">√(1 + |∇<i>g</i>|²) <i>dx dy</i></span>.</p>
<p>For <strong>flux</strong> the normal keeps its direction as well as its length:</p>
<div class="eqb"><span class="mth">∬<sub><i>S</i></sub> <i>F</i> <span class="op">·</span> <i>d</i><b>S</b> <span class="op">=</span> ∬<sub><i>D</i></sub> <i>F</i>(<b>r</b>(<i>u</i>,<i>v</i>)) <span class="op">·</span> (<b>r</b><sub><i>u</i></sub> <span class="op">×</span> <b>r</b><sub><i>v</i></sub>) <i>du dv</i></span></div>
<p>A scalar surface integral does not care about orientation, because <span class="mth"><i>dS</i></span> is an area. A flux integral does, and reversing the normal negates it exactly — so "the flux through a surface" is only meaningful once you have said which side is out. A Möbius band admits no consistent choice at all, which is precisely why flux through it is undefined: <em>orientability</em> is a real condition, not a technical nicety.</p>

<h3 id="t6">Stokes' theorem and the divergence theorem</h3>
<p>Both definitions were local limits. Each has a global counterpart obtained by tiling a large region with small ones: interior contributions cancel in pairs, leaving only the boundary.</p>
<h4>Divergence theorem (Gauss)</h4>
<div class="eqb"><span class="mth">∭<sub><i>V</i></sub> (∇<span class="op">·</span><i>F</i>) <i>dV</i> <span class="op">=</span> ∯<sub><i>∂V</i></sub> <i>F</i> <span class="op">·</span> n̂ <i>dS</i></span></div>
<p>Chop <span class="mth"><i>V</i></span> into tiny cells. Each contributes its own flux, which by the definition is about <span class="mth">(∇<span class="op">·</span><i>F</i>)Δ<i>V</i></span>. Where two cells meet, the shared face is counted twice with opposite normals and cancels. Only the outer boundary survives.</p>
<h4>Stokes' theorem</h4>
<div class="eqb"><span class="mth">∬<sub><i>Σ</i></sub> (∇<span class="op">×</span><i>F</i>) <span class="op">·</span> n̂ <i>dS</i> <span class="op">=</span> ∮<sub><i>∂Σ</i></sub> <i>F</i> <span class="op">·</span> <i>d</i><b>r</b></span></div>
<p>The same argument with loops: tile a surface with small ones, adjacent edges are traversed in opposite directions and cancel, only the boundary curve remains.</p>
${stThm('Divergence theorem (Gauss–Ostrogradsky)', {
  hyp:'<span class="mth"><i>V</i></span> is a bounded solid whose boundary <span class="mth">∂<i>V</i></span> is a piecewise-smooth closed surface with outward normal, and <span class="mth"><i>F</i></span> has continuous first partials on an open set containing <span class="mth"><i>V</i></span>',
  then:'',
  eq:'∭<sub><i>V</i></sub> (∇<span class="op">·</span><i>F</i>) <i>dV</i> <span class="op">=</span> ∯<sub><i>∂V</i></sub> <i>F</i> <span class="op">·</span> n̂ <i>dS</i>',
  proof:`<p>It is enough to prove the third component, <span class="mth">∭ ∂<i>R</i>/∂<i>z</i> <i>dV</i> = ∯ <i>R</i> k̂ · n̂ <i>dS</i></span>; the other two are the same argument along the other axes, and adding all three gives the theorem.</p>
<p>Take <span class="mth"><i>V</i></span> to be <span class="mth"><i>z</i></span>-simple: <span class="mth">(<i>x</i>,<i>y</i>) ∈ <i>D</i></span> and <span class="mth"><i>u</i>₁(<i>x</i>,<i>y</i>) ≤ <i>z</i> ≤ <i>u</i>₂(<i>x</i>,<i>y</i>)</span>. A general solid is cut into such pieces, and each internal cut is shared by two pieces with opposite outward normals, so those contributions cancel in pairs. That cancellation <em>is</em> the tiling argument, made exact.</p>
<p>By Fubini and the Fundamental Theorem in <span class="mth"><i>z</i></span>,</p>
${stEq('∭<sub><i>V</i></sub> <span class="frac"><span class="nm">∂<i>R</i></span><span class="den">∂<i>z</i></span></span> <i>dV</i> <span class="op">=</span> ∬<sub><i>D</i></sub> [ <i>R</i>(<i>x</i>,<i>y</i>,<i>u</i>₂) <span class="op">−</span> <i>R</i>(<i>x</i>,<i>y</i>,<i>u</i>₁) ] <i>dA</i>')}
<p>For the surface side, the boundary has a top <span class="mth"><i>z</i> = <i>u</i>₂</span>, a bottom <span class="mth"><i>z</i> = <i>u</i>₁</span>, and possibly a vertical side wall. On the side wall the outward normal is horizontal, so <span class="mth">k̂ · n̂ = 0</span> and it contributes nothing.</p>
<p>On the top, the outward normal points upward and <span class="mth">k̂ · n̂ <i>dS</i> = +<i>dA</i></span> after projecting to <span class="mth"><i>D</i></span>; on the bottom it points downward and gives <span class="mth">−<i>dA</i></span>. So the flux is <span class="mth">∬<sub><i>D</i></sub> [<i>R</i>(<i>x</i>,<i>y</i>,<i>u</i>₂) − <i>R</i>(<i>x</i>,<i>y</i>,<i>u</i>₁)] <i>dA</i></span> — the same expression.</p>`,
  note:'The pattern is worth naming: Fubini reduces the volume integral to an inner one-dimensional integral, the FTC evaluates that at the two ends, and the two ends are exactly the two pieces of the boundary. Every theorem in this family is that move in one more dimension.',
  see:'vector:2.3', seeLabel:'The divergence theorem, both sides computed' })}

${stThm("Stokes' theorem", {
  hyp:'<span class="mth">Σ</span> is a piecewise-smooth oriented surface whose boundary <span class="mth">∂Σ</span> is a simple closed curve oriented consistently by the right-hand rule, and <span class="mth"><i>F</i></span> has continuous first partials near <span class="mth">Σ</span>',
  then:'',
  eq:'∬<sub>Σ</sub> (∇<span class="op">×</span><i>F</i>) <span class="op">·</span> n̂ <i>dS</i> <span class="op">=</span> ∮<sub>∂Σ</sub> <i>F</i> <span class="op">·</span> <i>d</i><b>r</b>',
  proof:`<p>Take <span class="mth">Σ</span> to be a graph <span class="mth"><i>z</i> = <i>g</i>(<i>x</i>,<i>y</i>)</span> over a plane region <span class="mth"><i>D</i></span> whose boundary is the curve <span class="mth"><i>C</i></span>; a general surface is assembled from such patches, and shared edges cancel.</p>
<p>Parametrise <span class="mth">∂Σ</span> by carrying <span class="mth"><i>C</i></span> up to the surface. On it <span class="mth"><i>z</i> = <i>g</i>(<i>x</i>,<i>y</i>)</span>, so by the chain rule <span class="mth"><i>dz</i> = <i>g</i><sub><i>x</i></sub> <i>dx</i> + <i>g</i><sub><i>y</i></sub> <i>dy</i></span>. Substituting into the line integral turns it into a purely two-dimensional one over <span class="mth"><i>C</i></span>:</p>
${stEq('∮<sub><i>C</i></sub> (<i>P</i> <span class="op">+</span> <i>R g</i><sub><i>x</i></sub>) <i>dx</i> <span class="op">+</span> (<i>Q</i> <span class="op">+</span> <i>R g</i><sub><i>y</i></sub>) <i>dy</i>')}
<p>Apply <strong>Green's theorem</strong> to this. The integrand becomes <span class="mth">∂<sub><i>x</i></sub>(<i>Q</i> + <i>Rg</i><sub><i>y</i></sub>) − ∂<sub><i>y</i></sub>(<i>P</i> + <i>Rg</i><sub><i>x</i></sub>)</span>, expanded with the chain rule because <span class="mth"><i>P</i>, <i>Q</i>, <i>R</i></span> depend on <span class="mth"><i>z</i> = <i>g</i></span> as well.</p>
<p>Two of the resulting terms are <span class="mth"><i>R g</i><sub><i>yx</i></sub></span> and <span class="mth"><i>R g</i><sub><i>xy</i></sub></span>, which cancel by equality of mixed partials. What survives is exactly</p>
${stEq('∬<sub><i>D</i></sub> [ <span class="op">−</span>(∇<span class="op">×</span><i>F</i>)<sub><i>x</i></sub> <i>g</i><sub><i>x</i></sub> <span class="op">−</span> (∇<span class="op">×</span><i>F</i>)<sub><i>y</i></sub> <i>g</i><sub><i>y</i></sub> <span class="op">+</span> (∇<span class="op">×</span><i>F</i>)<sub><i>z</i></sub> ] <i>dA</i>')}
<p>and <span class="mth">⟨−<i>g</i><sub><i>x</i></sub>, −<i>g</i><sub><i>y</i></sub>, 1⟩ <i>dA</i></span> is precisely <span class="mth">n̂ <i>dS</i></span> for the upward-oriented graph. So this is <span class="mth">∬<sub>Σ</sub> (∇×<i>F</i>) · n̂ <i>dS</i></span>.</p>
<p>Green's theorem is therefore not an analogy for Stokes' theorem — it is the engine of its proof.</p>`,
  note:'Orientation is a hypothesis with teeth. The right-hand rule ties the direction of n̂ to the direction of travel round ∂Σ; reverse one without the other and the identity becomes a sign error. A Möbius band admits no consistent n̂ at all, so the theorem simply does not apply to it.',
  see:'vector:2.2', seeLabel:"Stokes' theorem — and the cap that does not matter" })}

<p>Both are cases of the general Stokes theorem <span class="mth">∫<sub><i>Ω</i></sub> <i>dω</i> = ∫<sub><i>∂Ω</i></sub> ω</span>, which also contains the fundamental theorem of calculus (<span class="mth">∫<sub><i>a</i></sub><sup><i>b</i></sup> <i>f</i>′ = <i>f</i>(<i>b</i>) − <i>f</i>(<i>a</i>)</span>) and the gradient theorem <span class="mth">∫<sub><i>C</i></sub> ∇<i>f</i> <span class="op">·</span> <i>d</i><b>r</b> = <i>f</i>(end) − <i>f</i>(start)</span>. Differentiate, then integrate over a region — you get back what happens on the boundary.</p>
<p class="note"><strong>The cap does not matter.</strong> Every surface with the same boundary gives the same flux of a curl, and the reason is the identity <span class="mth">∇<span class="op">·</span>(∇<span class="op">×</span><i>F</i>) = 0</span>: a curl is divergence-free, so its flux through any <em>closed</em> surface vanishes — and gluing two caps with a common boundary makes exactly such a closed surface. That is what makes Stokes' theorem useful in practice: replace a hard surface with any easier one sharing its edge. The lab offers four caps of visibly different areas and prints the four fluxes as one number.</p>
<p class="note">The <strong>Irrotational vortex</strong> shows why the hypotheses matter. Its curl is zero everywhere it is defined, so Stokes seems to force every circulation to vanish — but a loop around the <span class="mth"><i>z</i></span>-axis has circulation <span class="mth">2π</span>. No contradiction: <em>every</em> surface spanning that loop must pass through the axis, where the field blows up, so the theorem does not apply. Domain topology is not a technicality here; it is the whole content.</p>

<h3 id="t7">Identities, and why they are true</h3>
<div class="eqb"><span class="mth">∇<span class="op">×</span>(∇<i>f</i>) <span class="op">=</span> <b>0</b></span></div>
<p>The <span class="mth"><i>z</i></span>-component is <span class="mth">∂<sub><i>x</i></sub>(∂<sub><i>y</i></sub><i>f</i>) − ∂<sub><i>y</i></sub>(∂<sub><i>x</i></sub><i>f</i>)</span>, which vanishes by equality of mixed partials (Clairaut–Schwarz, given continuous second derivatives). Gradient fields are exactly the <strong>conservative</strong> fields: work done depends only on the endpoints. Load any scalar preset and press <strong>Send ∇f to F</strong> — the curl readout collapses to a symbolic zero, computed, not asserted.</p>
<div class="eqb"><span class="mth">∇<span class="op">·</span>(∇<span class="op">×</span><i>F</i>) <span class="op">=</span> 0</span></div>
<p>Expanding gives six third-order terms that cancel in pairs, again by mixed partials. Curl fields are <strong>source-free</strong>. Press <strong>Send ∇×F to F</strong> and watch the divergence vanish identically.</p>
<div class="eqb"><span class="mth">∇<span class="op">·</span>(∇<i>f</i>) <span class="op">=</span> ∇²<i>f</i> <span class="op">=</span> <span class="frac"><span class="nm">∂²<i>f</i></span><span class="den">∂<i>x</i>²</span></span> <span class="op">+</span> <span class="frac"><span class="nm">∂²<i>f</i></span><span class="den">∂<i>y</i>²</span></span> <span class="op">+</span> <span class="frac"><span class="nm">∂²<i>f</i></span><span class="den">∂<i>z</i>²</span></span></span></div>
<p>The <strong>Laplacian</strong>, the trace of the Hessian. It compares <span class="mth"><i>f</i></span> at a point with its average on a small sphere around that point: negative at a peak, positive in a basin, zero when the value is exactly the local average. Functions with <span class="mth">∇²<i>f</i> = 0</span> are called <strong>harmonic</strong>; <span class="mth">1/<i>r</i></span> is the standard example away from the origin, which is why it is the potential of a point charge.</p>
<p>Useful product rules, all provable by expanding components:</p>
<ul>
  <li><span class="mth">∇<span class="op">·</span>(<i>f F</i>) <span class="op">=</span> <i>f</i>(∇<span class="op">·</span><i>F</i>) <span class="op">+</span> <i>F</i> <span class="op">·</span> ∇<i>f</i></span></li>
  <li><span class="mth">∇<span class="op">×</span>(<i>f F</i>) <span class="op">=</span> <i>f</i>(∇<span class="op">×</span><i>F</i>) <span class="op">+</span> ∇<i>f</i> <span class="op">×</span> <i>F</i></span></li>
  <li><span class="mth">∇<span class="op">·</span>(<i>F</i> <span class="op">×</span> <i>G</i>) <span class="op">=</span> <i>G</i> <span class="op">·</span> (∇<span class="op">×</span><i>F</i>) <span class="op">−</span> <i>F</i> <span class="op">·</span> (∇<span class="op">×</span><i>G</i>)</span></li>
  <li><span class="mth">∇<span class="op">×</span>(∇<span class="op">×</span><i>F</i>) <span class="op">=</span> ∇(∇<span class="op">·</span><i>F</i>) <span class="op">−</span> ∇²<i>F</i></span></li>
</ul>
<p>The last two identities together with the first two are the backbone of the Helmholtz decomposition: under mild conditions any field splits into a curl-free part (a gradient) plus a divergence-free part (a curl). Divergence and curl between them capture everything local about a field.</p>

`;
