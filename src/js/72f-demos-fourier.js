/* ============================================================================
   6f · THE FOURIER WING
   ============================================================================ */

const FOURIER_GROUPS = [
{ g:'Building things out of sines', items:[
  {n:'The Fourier series — a square wave from sines', ex:'x(t) = Σ b_k sin(2πkt)', stage:'ftSynth',
   opts:{ kind:'square', K:7 },
   out:'Seven harmonics already look like a square wave. Drag the slider up and the flat parts flatten and the edges sharpen — but the overshoot at each jump refuses to shrink. It settles at 1.1790, which is 8.95% of the step, and stays there however many terms you add.',
   note:'The claim is that <b>any</b> periodic function is a sum of sines and cosines at multiples of its fundamental. Finding the recipe is easier than it looks, because sines at different frequencies are <b>orthogonal</b>: <b>∫₀¹ sin(2πmt)sin(2πnt)dt = 0</b> unless m = n. So multiply the mystery function by sin(2πkt), average over a period, and every term except the k-th annihilates itself — leaving <b>b_k = 2∫₀¹ x(t)sin(2πkt)dt</b>. It is a dot product with a basis vector, in a space whose vectors are functions. Compare the three waveforms: a square and a sawtooth have coefficients falling as 1/k, a triangle as 1/k². Smoother functions need fewer harmonics, and that is a general rule — the rate at which coefficients decay measures how differentiable a function is.'},
  {n:'Gibbs — the overshoot that never goes away', ex:'(2/π)Si(π) = 1.1790, forever', stage:'ftSynth',
   opts:{ kind:'square', K:61, showParts:false },
   out:'Sixty-one harmonics. The approximation is excellent everywhere except within a hair of the jump, where it still overshoots by the same 9% it did with seven. What has changed is the width of the overshoot, not its height.',
   note:'This is not a numerical artefact and not a failure of the series — it converges perfectly well, just not <i>uniformly</i>. At every fixed point away from the jump the partial sums converge to the right value. But the point where the maximum overshoot occurs moves closer to the jump as you add terms, so it is never the same point twice, and the height there tends to <b>(2/π)Si(π) = 1.178979…</b> rather than to 1. The readout measures both the height and the width to show exactly that: the height is stuck, the width collapses. Any attempt to represent a discontinuity with continuous functions pays this price — which is why sharp edges are expensive in image compression and why brick-wall filters ring.'}
]},
{ g:'Why the transform works', items:[
  {n:'The winding machine', ex:'X(f) = ∫x(t)e^(−2πift)dt, drawn', stage:'ftWind',
   opts:{ f1:3, f2:5, a2:0.7, sweep:true },
   out:'The signal is wrapped around a circle at a winding frequency you control, and the centre of mass of the wound shape is marked. Almost everywhere it sits at the origin. At 3 Hz and at 5 Hz — the two frequencies the signal is actually made of — it lurches out. The swept plot underneath is the transform.',
   note:'This is the whole mechanism, with no algebra hidden. Multiplying by <b>e^(−2πift)</b> is a rotation at f turns per second, so <b>x(t)e^(−2πift)</b> is the signal wound around the origin. Integrating is averaging. If f matches nothing in the signal, each hump of the signal is cancelled by another hump half a turn away and the average lands on zero — that is orthogonality, seen rather than proved. If f matches a component, every hump of that component lands on the same side of the circle and they reinforce instead. <b>The transform is the centre of mass of the wound signal, as a function of winding frequency.</b> Watch the 0 Hz spike too: a constant offset never rotates at all, so it always contributes — that is the DC term.'}
]},
{ g:'The two domains are one object', items:[
  {n:'Transform pairs and duality', ex:'narrow ⟷ wide, always', stage:'ftPairs',
   opts:{ kind:'gauss', width:1 },
   out:'A Gaussian transforms into a Gaussian — the one shape that is its own transform. Squeeze it in time and its transform spreads in exactly compensating measure; the panel measures both widths from the curves and multiplies them.',
   note:'Three pairs worth knowing by heart: <b>Gaussian ⟷ Gaussian</b>, <b>rectangle ⟷ sinc</b>, and <b>two-sided exponential ⟷ Lorentzian</b> (the shape of a spectral line). All three show the same thing: compactness in one domain costs spread in the other, and the product of the widths satisfies <b>Δt·Δf ≥ 1/4π</b> with equality only for the Gaussian. Multiply through by ħ and you have <b>Δx·Δp ≥ ħ/2</b> — the quantum uncertainty principle is this theorem plus the identification <b>p = ħk</b>. Nothing quantum has been added; the quantum wing\'s wave packet is the same mathematics wearing different units. The rectangle is the cautionary tale: perfectly compact in time, with a transform whose tails fall only as 1/f and never terminate.'},
  {n:'Discrete or continuous — sampling, aliasing, leakage', ex:'what changes when you only have dots', stage:'ftDiscrete',
   opts:{ f:3, fs:32, win:'rect' },
   out:'A tone, the samples taken of it, and the discrete transform of those samples. Push the tone above half the sample rate and it folds back down — the dots become identical to those of a lower tone, and the spectrum shows the impostor rather than the original.',
   note:'The continuous transform integrates over all time and returns a function of a continuous frequency; the DFT sums N samples and returns N numbers. Two consequences follow, and every practical difficulty in spectrum analysis is one of them. <b>Sampling in time makes the spectrum periodic</b>, so frequencies above <b>f_s/2</b> are indistinguishable from ones below — <b>aliasing</b>, and irreversible, which is why converters filter before they sample. <b>Truncating in time smears the spectrum</b>, because the DFT assumes your N samples repeat forever and any mismatch at the join is a discontinuity with harmonics everywhere — <b>leakage</b>. Nudge the tone off an exact bin and watch clean lines turn into skirts, then switch the window from rect to Hann and watch the skirts collapse. <b>Nyquist–Shannon</b> is the good news in all this: sample faster than twice the highest frequency present and <i>nothing at all</i> is lost.'}
]},
{ g:'The algorithms', items:[
  {n:'The fast Fourier transform', ex:'N² becomes ½N log₂N', stage:'ftFast',
   opts:{ p:6 },
   out:'Both algorithms compute the same sum. The direct definition costs N² operations; the FFT costs ½N log₂N. Press "Time them both" to run each one here and now — they agree to rounding error, and one is enormously faster.',
   note:'The trick is a single observation. Split the sum into even-numbered and odd-numbered samples, and each half is <i>itself</i> a transform of half the length, because <b>e^(−2πik(2m)/N) = e^(−2πikm/(N/2))</b>. Better still, the answers for bin <b>k</b> and bin <b>k + N/2</b> reuse exactly the same two sub-transforms, differing only in the sign of one twiddle factor — so they come in pairs, the <b>butterflies</b>. Recursing gives log₂N levels of N/2 butterflies each. Nothing is approximated; the algorithm just stops recomputing what it already knows. Cooley and Tukey published it in 1965, Gauss had it in 1805 and never printed it, and it is not an exaggeration to say that digital signal processing, medical imaging and mobile telephony all wait on it.'},
  {n:'The inverse — and lossy compression', ex:'x = F⁻¹F x, exactly', stage:'ftInverse',
   opts:{ kind:'noisy', keep:8 },
   out:'Transform, keep only the largest few coefficients, transform back. With everything kept the reconstruction is exact to machine precision. With eight of sixty-five kept, a noisy tone comes back almost perfectly — because the tone is concentrated in a couple of bins while the noise is spread thinly over all of them.',
   note:'The forward and inverse transforms differ only in the sign of the exponent and a factor of 1/N: <b>x[n] = (1/N)ΣX[k]e^(+2πikn/N)</b>. A transform is a change of basis, and a change of basis is invertible by construction — so no information is lost until you choose to lose some. Choosing well is what compression is: <b>JPEG</b> transforms 8×8 blocks with a cosine transform and discards the small coefficients; <b>MP3</b> does the same to slices of audio, with the refinement of also discarding what the ear cannot hear. Try each signal. The <b>pulse</b> compresses badly and rings when truncated — Gibbs again, arriving by another route. The <b>noisy tone</b> compresses beautifully, and keeping only the peaks is incidentally a rather good denoiser.'},
  {n:'The convolution theorem', ex:'x ∗ h ⟷ X · H', stage:'ftConv',
   opts:{ cut:6 },
   out:'The same filtering computed two entirely different ways — by sliding one function across another and integrating, and by multiplying two spectra bin by bin. The two curves lie exactly on top of each other, and the panel reports how far apart they are: about 10⁻¹⁶.',
   note:'Convolution — <b>y(t) = ∫x(τ)h(t−τ)dτ</b> — is what every linear time-invariant system does to its input, and computed directly it costs N² operations. In the frequency domain it is a single multiplication per bin. Two forward transforms, N multiplications and an inverse: that is fast convolution, and it is how long filters are applied to audio, how large images are blurred, and how very large integers are multiplied. It runs the other way too — multiplying two signals in time <i>convolves</i> their spectra, which is exactly what modulating a radio carrier does, and what a window does to a spectral line. This is also the bridge back to the circuits wing: a circuit\'s transfer function <b>H(jω)</b>, the thing its Bode plot draws, <i>is</i> the Fourier transform of its impulse response. The two wings have been describing one object all along.'}
]}];

