const THEORY_VC_4 = `<h3 id="t12">Mechanics and quantum — the same operators, one more time</h3>
<h4>Newton's second law as the bridge</h4>
<p>The test-particle instrument closes the loop between fields and motion. A field stops being a picture and becomes a <em>cause</em>:</p>
<div class="eqb"><span class="mth"><i>m</i><b>ẍ</b> <span class="op">=</span> <i>F</i>(<b>x</b>) , &nbsp;&nbsp; and for a potential &nbsp; <i>F</i> <span class="op">=</span> −∇<i>U</i></span></div>
<p>Kinematics is what the integrator draws: uniform force ⇒ parabola (projectiles); linear restoring force ⇒ ellipse with amplitude-independent period (SHM); inverse-square attraction ⇒ conic sections (Kepler). Two conservation laws come from the two structures a force can have — <strong>energy</strong> from being a gradient (<span class="mth">dE/dt = <i>m</i><b>v</b>·<b>a</b> + ∇<i>U</i>·<b>v</b> = 0</span>), and <strong>angular momentum</strong> from being central (<span class="mth">τ = <b>r</b>×<i>F</i> = 0</span>). The lopsided-bowl demo breaks the second while keeping the first, and you watch exactly one of the two readouts start to breathe.</p>
<h4>The Lorentz force: geometry doing the work of algebra</h4>
<div class="eqb"><span class="mth"><i>F</i> <span class="op">=</span> <i>q</i>(<i>E</i> <span class="op">+</span> <b>v</b> <span class="op">×</span> <i>B</i>)</span></div>
<p>Because <span class="mth"><b>v</b>×<i>B</i> ⊥ <b>v</b></span>, magnetic forces change direction but never speed — the KE readout is flat while the path curls. Everything AP E&amp;M asks about charged-particle motion falls out of components: perpendicular velocity circles at <span class="mth"><i>r</i> = <i>m v</i><sub>⊥</sub>/<i>qB</i></span>, parallel velocity is untouched (helices), and a crossed uniform <span class="mth"><i>E</i></span> cancels the magnetic push at exactly <span class="mth"><i>v</i> = <i>E</i>/<i>B</i></span> — the velocity selector. Gravity, meanwhile, is the inverse-square law you already met as the point charge with the sign forced attractive: one formula, two forces, which is why the builder's point mass and point charge write almost the same expression.</p>
<h4>Quantum mechanics: the Laplacian becomes an energy meter</h4>
<p>The time-independent Schrödinger equation,</p>
<div class="eqb"><span class="mth">−<span class="frac"><span class="nm">ℏ²</span><span class="den">2<i>m</i></span></span>∇²ψ <span class="op">+</span> <i>V</i>ψ <span class="op">=</span> <i>E</i>ψ &nbsp;&nbsp;⇒&nbsp;&nbsp; <span class="frac"><span class="nm">∇²ψ</span><span class="den">ψ</span></span> <span class="op">=</span> <span class="frac"><span class="nm">2<i>m</i></span><span class="den">ℏ²</span></span>(<i>V</i> − <i>E</i>)</span></div>
<p>says that for an eigenstate, the <em>curvature per unit wavefunction</em> is fixed by the energy budget at that point. That turns this tool's Laplacian readout into a physics instrument: probe a box eigenstate and <span class="mth">∇²ψ/ψ</span> is the same constant everywhere (the eigenvalue); probe the hydrogen 2p orbital and it traces <span class="mth">¼ − 2/<i>r</i></span> exactly; colour the map by <span class="mth">∇²ψ</span> and the sign flip marks the <strong>classical turning point</strong>, with tunnelling beyond it. Where <span class="mth"><i>V</i> &lt; <i>E</i></span> the state curves toward zero and oscillates; where <span class="mth"><i>V</i> &gt; <i>E</i></span> it curves away and decays. And superposition — the same superposition that built dipoles and antenna arrays — makes quantum <em>dynamics</em>: two eigenstates beating at <span class="mth">Δ<i>E</i></span> slosh the probability density, which is an atom mid-transition.</p>
<h4>The uncertainty principle is a statement about curvature</h4>
<p>Localising a wavepacket to width σ forces its curvature up: for a Gaussian, <span class="mth">∇²ψ/ψ</span> at the peak is <span class="mth">−1/σ²</span> per dimension. Since <span class="mth">−∇²</span> is the kinetic operator, momentum spread scales as <span class="mth">Δp ∝ 1/σ</span> while <span class="mth">Δx ∝ σ</span> — the product has a floor, <span class="mth">Δx·Δp ≥ ℏ/2</span>. The Heisenberg demo lets you pay the price live: shrink σ in the formula and watch the eigenvalue meter deepen as 1/σ². This is not a measurement-disturbance story; it is Fourier analysis wearing physics clothes, and it is why matter is stable — pinning an electron to the nucleus would cost unbounded kinetic energy.</p>
<h4>Wells, bound states, and tunnelling</h4>
<p>A bound state has <span class="mth"><i>E</i> &lt; <i>V</i>(∞)</span>: outside its well the wavefunction cannot oscillate, only decay, with decay rate set by how far below the barrier it sits. The delta-well demo shows the cleanest case — <span class="mth">ψ = e<sup>−|x|</sup></span>, where <em>everywhere</em> outside the single point of attraction is forbidden territory and the eigenvalue meter reads a constant +1 (pure decay). The oscillator demo shows the generic case: oscillation inside the classical turning point, decay outside, with the boundary visible as the sign change of <span class="mth">∇²ψ</span>. That leaking tail is tunnelling — alpha decay, tunnel diodes, and the scanning-tunnelling microscope all live in it.</p>
<h4>Blackbody radiation: where quantum mechanics began</h4>
<p>Classical physics predicted a radiating cavity's spectrum as <span class="mth">u ∝ ν²T</span> — growing without bound at high frequency, the <em>ultraviolet catastrophe</em>. Planck's fix was to quantise the oscillators, which multiplies in the factor <span class="mth">1/(e<sup>hν/kT</sup> − 1)</span>:</p>
<div class="eqb"><span class="mth"><i>u</i>(ν, <i>T</i>) ∝ <span class="frac"><span class="nm">ν³</span><span class="den"><i>e</i><sup>hν/kT</sup> − 1</span></span></span></div>
<p>The blackbody demo draws this as terrain over the (frequency, temperature) plane: each constant-T line is a spectrum, the crest is Wien's law <span class="mth">ν<sub>peak</sub> ∝ T</span> (why hotter things glow bluer), and the area under each line grows as T⁴ (Stefan–Boltzmann, why they glow <em>much</em> brighter). Type the classical ν²T instead and the catastrophe is a surface that never comes back down.</p>
<h4>Qubits and quantum computing</h4>
<p>A qubit is nothing more exotic than the two-state superposition this page already animates: <span class="mth">α|0⟩ + β|1⟩</span>, with the physics living in the amplitudes' relative <strong>phase</strong>. The two-antenna interference demo is the whole mechanism in miniature — two coherent amplitudes, a phase knob, and a fringe pattern that moves when the phase does. A quantum computation prepares a superposition over all 2<sup>N</sup> states of N qubits, uses gates to rotate phases so that paths to wrong answers cancel and paths to right answers reinforce, and then measures — sampling the fringe pattern. Grover's search and Shor's factoring are phase choreography of exactly this kind. The catch is <strong>decoherence</strong>: stray coupling to the environment randomises the phases, the fringes wash out, and the machine degrades into a slow classical one — which is why quantum computers live in dilution refrigerators. Note also the resource accounting: N qubits span 2<sup>N</sup> amplitudes, a "field over parameter space" far too large to store classically — the same idea as this page's loss surfaces, at a scale nothing classical can draw.</p>
<p class="note">What is honestly shown: real-valued wavefunctions and probability densities, exact eigen-relations, and the beat dynamics of superpositions (where only the phase <em>difference</em> matters, so a real cosine tells the truth). What is not: complex phase on its own, spin, entanglement's nonlocal correlations, and measurement collapse.</p>
<h4>Where each AP topic lives</h4>
<ul>
  <li><strong>Kinematics &amp; Newton's laws</strong> — test particle: N1 (zero field), projectile, impulse via the velocity readout.</li>
  <li><strong>Energy &amp; work</strong> — potential-mode particle: E = KE + U frozen; magnetic forces doing zero work.</li>
  <li><strong>Circular motion, gravitation, Kepler</strong> — orbit, escape-velocity and binary-mass demos; L<sub>z</sub> as the equal-area law.</li>
  <li><strong>Oscillations &amp; waves</strong> — SHM bowl, standing wave, travelling antenna wave, interference and beats (two antennas / sloshing).</li>
  <li><strong>Electrostatics</strong> — charges, Gauss's law flux box, equipotentials, dipoles, charge + uniform field.</li>
  <li><strong>Magnetism &amp; induction</strong> — wires, Ampère loops, magnetic dipole, Faraday's curl, cyclotron/helix/velocity selector.</li>
  <li><strong>Fluids &amp; thermo</strong> — incompressible whirlpool, buoyancy as −∇P, Fourier heat flow.</li>
  <li><strong>Modern</strong> — box states, superposition dynamics, a real hydrogen orbital, tunnelling past the turning point.</li>
  <li><strong>Deliberately absent</strong> — circuits, two-body collisions, ray optics, relativity: they are not vector fields over space, and this laboratory does not pretend otherwise.</li>
</ul>

<h3 id="t8">Reading the pictures</h3>
<ul>
  <li><strong>Field arrows</strong> sample the field on a lattice. Colour is magnitude; length is magnitude too, but log-compressed by default so that tiny and enormous vectors are visible in the same picture. Switch to <em>true</em> for honest proportions or <em>equal</em> to read direction alone.</li>
  <li><strong>Field lines</strong> are continuous curves everywhere tangent to the field, with arrowheads along the flow — the picture E and B are usually drawn with. When the field was built from objects, lines are seeded on the sources, so they visibly leave + charges and dive into − ones. Tangency is exact (integrated, not sketched); line <em>spacing</em> is only qualitative, since a finite drawing cannot make density track strength perfectly.</li>
  <li><strong>Flow lines</strong> integrate <span class="mth">ẋ = <i>F</i>(<b>x</b>)</span> with fourth-order Runge–Kutta. Sources spray particles outward, sinks swallow them, vortices trap them in orbits. In scalar mode they flow uphill along <span class="mth">∇<i>f</i></span>.</li>
  <li><strong>Colour slice</strong> paints a plane by a scalar — the field itself, its divergence, or a magnitude. Signed quantities use a diverging scale centred on zero, so the sign is visible at a glance.</li>
  <li><strong>Level sets</strong> are surfaces of constant <span class="mth"><i>f</i></span>, drawn as a stack of contour slices. The one thing to check: gradient arrows always cross them at right angles.</li>
  <li><strong>Flux box</strong> and <strong>circulation loop</strong> evaluate the defining integrals numerically. The numbers they print are computed without ever using the symbolic derivatives, so when they agree with <span class="mth">∇<span class="op">·</span><i>F</i></span> and <span class="mth">(∇<span class="op">×</span><i>F</i>)<span class="op">·</span>n̂</span>, that agreement is real evidence rather than a tautology.</li>
</ul>
<p>Everything on screen is differentiated <strong>symbolically</strong> from whatever you type — the chain, product and quotient rules applied to a parsed expression tree — so the derivations in the panel are the actual working, not a numerical approximation dressed up as algebra.</p>
`;

/* The essay is assembled from its four sections; edit a section in place. */
const THEORY = THEORY_VC_1 + THEORY_VC_2 + THEORY_VC_3 + THEORY_VC_4;
