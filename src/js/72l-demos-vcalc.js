const VCALC_STAGE_GROUPS = [
{ g:'Line integrals and conservative fields', items:[
  {n:'Line integrals: the curtain and the work', ex:'∫f ds and ∫F·dr, accumulated live', stage:'vcLineInt',
   opts:{kind:'work', path:'circle', fld:'rot'},
   out:'The lower plot is the integral being accumulated as the point runs the curve; its final value is the answer. The panel computes all three integrals over the same curve at once.',
   note:'A scalar line integral uses ds = |r′|dt, which is positive whichever way you go — reversing the curve does not change it. A vector line integral uses dr = r′dt, which reverses. That is not a technicality: work done against a force is the negative of work done by it, and the sign is the whole point.'},
  {n:'Path independence, tested three ways', ex:'same endpoints, three different routes', stage:'vcConserv',
   opts:{fld:'grad'},
   out:'Three genuinely different curves, each integrated on its own. For a gradient field they agree to quadrature precision; for a rotation field they do not, and the spread is exactly the circulation around the loop the two paths make together.',
   note:'Four statements are equivalent on a simply connected domain, and the panel checks them one at a time: F = ∇f; ∮F·dr = 0 around every closed curve; work is path-independent; and P<sub>y</sub> = Q<sub>x</sub>. The last is the cheap test — and the one with a catch.'},
  {n:'The catch: a hole in the domain', ex:'zero curl everywhere, circulation 2π anyway', stage:'vcConserv',
   opts:{fld:'vortex'},
   out:'The cross-partial test passes at every point where the field is defined, and yet the circulation around the unit circle is exactly 2π. The loop cannot be shrunk to a point without leaving the domain.',
   note:'The field is <i>locally</i> a gradient — a potential exists on any disc avoiding the origin — but not globally: the potential is the polar angle, which increases by 2π every time round. Winding numbers, branch cuts and the Aharonov–Bohm phase all live in this gap, and the "simply connected" clause in the theorem is doing real work.'},
  {n:'Recovering the potential', ex:'integrate along a staircase, twice', stage:'vcConserv',
   opts:{fld:'radial'},
   out:'Integrate P along a horizontal leg then Q along a vertical one; do it in the other order; the two agree precisely when the cross-partial test passes. The work along any path then equals f(end) − f(start).',
   note:'That is the <b>Fundamental Theorem for line integrals</b>: the one-variable Fundamental Theorem with the interval replaced by a curve. It is why potential energy is a useful idea at all — and why "the work done by gravity" can be quoted without asking which route you took.'}
]},
{ g:"Green's theorem", items:[
  {n:'Circulation form, both sides measured', ex:'∮P dx + Q dy = ∬(Q<sub>x</sub> − P<sub>y</sub>) dA', stage:'vcGreen',
   opts:{fld:'rot', path:'circle', form:'circ'},
   out:'The line integral is a one-dimensional adaptive quadrature along a parametrised curve; the double integral is a two-dimensional Gauss rule over the region. They share no code and no assumptions, and the panel prints their difference.',
   note:'The little loops drawn inside the region are the argument, not an illustration of it. Each carries its own circulation; the arrows on shared edges point opposite ways and cancel. What survives is the boundary — and the theorem is the statement that the sum of the insides equals what is left at the edge.'},
  {n:'Flux form — the divergence theorem in the plane', ex:'∮F·n̂ ds = ∬(P<sub>x</sub> + Q<sub>y</sub>) dA', stage:'vcGreen',
   opts:{fld:'radial', path:'ellipse', form:'flux'},
   out:'The same theorem applied to the field rotated by 90°. Outward flux across the boundary equals the total divergence inside, and both sides are computed separately.',
   note:'One theorem, two costumes. It is the two-dimensional ancestor of both Stokes\' theorem and the divergence theorem, which is exactly why those two look so alike — and why proving one of them essentially proves the other.'},
  {n:'The planimeter: area from the boundary alone', ex:'A = ½∮(x dy − y dx)', stage:'vcGreen',
   opts:{fld:'rot', path:'cardioid', form:'area'},
   out:'Set Q<sub>x</sub> − P<sub>y</sub> = 1 and Green\'s theorem returns the area — computed without ever visiting the interior. Check the cardioid against 3πa²/2 and the square against 4a².',
   note:'A planimeter is a mechanical instrument that evaluates this integral by being wheeled once around the outline of a shape. The same integral run on the vertices of a polygon is the shoelace formula every mapping package uses. Note the orientation: reverse the boundary and both sides flip sign together.'}
]},
{ g:'Surfaces, Stokes and the divergence theorem', items:[
  {n:'Surface integrals and the area element', ex:'dS = |r_u × r_v| du dv', stage:'vcSurface',
   opts:{surf:'hemisphere', kind:'area'},
   out:'The two tangent vectors span the tangent plane, and the area of the parallelogram they span is dS. The computed area is checked against the known one for every surface in the list — sphere 4π, cone π√2, torus 4π²Ra by Pappus.',
   note:'This is the Jacobian of the integration wing, one dimension up and one dimension out: the same cross-product-as-area-scale, now measuring how a flat parameter rectangle is stretched onto a curved surface.'},
  {n:'Flux, and why orientation is a choice', ex:'∬F·dS with the normal kept, not just its length', stage:'vcSurface',
   opts:{surf:'sphere', kind:'flux', fld:'radial'},
   out:'Reversing the normal negates the flux exactly. A scalar surface integral does not care about orientation; a flux integral does.',
   note:'"The flux through a surface" is only meaningful once you have said which side is out. A Möbius band admits no consistent choice at all, which is precisely why flux through it is undefined — orientability is a real condition, not a technical nicety.'},
  {n:"Stokes' theorem — and the cap that does not matter", ex:'∮F·dr = ∬(∇×F)·dS', stage:'vcStokes',
   opts:{cap:'hemisphere', fld:'swirl'},
   out:'Four different caps share the same boundary circle. Their areas differ by a factor of two; the flux of the curl through all of them agrees to quadrature precision.',
   note:'The reason is one identity: <b>∇·(∇×F) = 0</b>, always. A curl is divergence-free, so its flux through any <i>closed</i> surface vanishes — and gluing two caps with a common boundary makes exactly such a closed surface. The panel checks that too, on a torus. Surface-independence is what makes Stokes\' theorem useful: replace a hard surface with any easier one sharing the boundary.'},
  {n:'The divergence theorem', ex:'∯F·dS = ∭(∇·F) dV', stage:'vcDiverg',
   opts:{fld:'radial'},
   out:'The sphere\'s skin is coloured by F·n̂ and the open wedge shows ∇·F on a slice. Both integrals are computed independently, on a sphere and again on a cylinder with its two caps.',
   note:'What leaves through the skin equals what is produced inside. Divergence is <i>defined</i> as the limit of flux per unit volume, so the theorem is the statement that these local rates add up correctly — and they do, by the same cancellation of shared faces that drives Green\'s theorem.'},
  {n:'Zero divergence, and flux anyway', ex:'the inverse-square field and its missing point', stage:'vcDiverg',
   opts:{fld:'inverse'},
   out:'∇·F is exactly zero at every point where the field is defined, and yet the flux is 4π through a sphere of any radius. All of the source is concentrated at the one point the field forgot to have a value at.',
   note:'That is what a delta function is, and what a point charge is. Gauss\'s law in the electromagnetism wing is this stage with units attached: the flux out of any closed surface counts the charge within, whatever the shape, because the shape cancels out of the divergence theorem.'}
]}];

