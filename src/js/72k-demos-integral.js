const INTEGRAL_GROUPS = [
{ g:'What an integral is', items:[
  {n:'The integral as a limit of sums', ex:'five rules, and the order each converges at', stage:'igRiemann',
   opts:{key:'sine', rule:'mid', n:8},
   out:'The dots are the sample points the rule actually used. The error panel measures the convergence rate rather than quoting it: log₂ of the ratio of errors at n and 2n lands on 1 for the endpoint rules, 2 for midpoint and trapezoid, and 4 for Simpson.',
   note:'The definite integral is <i>defined</i> as this limit — not as "the antiderivative evaluated at the ends", which is a theorem that arrives later and is not even available for every integrable function. Notice that the midpoint rule beats the trapezoid rule by exactly a factor of two: its over- and under-shoots on the two halves of each panel partly cancel.'},
  {n:'When the theory stops applying', ex:'√x, whose derivative blows up at the endpoint', stage:'igRiemann',
   opts:{key:'root', rule:'simpson', n:8},
   out:'Simpson\'s measured order collapses from 4 to about 1.5. The error bounds depend on derivatives that are not bounded here, so the theorem simply does not apply — and the observed number says so honestly.',
   note:'Every error bound in the panel is printed with a <b>measured</b> K rather than a guessed one: the routine samples |f″| and |f⁗| over the interval and uses the maximum. All the bounds hold, most by a wide margin, because a bound is a worst case and the worst case rarely happens.'},
  {n:'The Fundamental Theorem, both parts', ex:"A(x) = ∫ₐˣ f, and A′(x) = f(x)", stage:'igFTC',
   opts:{key:'sine'},
   out:'The orange sliver is what one more step of x adds: width dx, height f(x). The lower plot is the accumulated area, built as a genuine running total, and its slope is differenced back and compared with f(x). They agree.',
   note:'Integration and differentiation undo each other, and that is not obvious — it is the single most important theorem in the subject. Part 2 turns it into an algorithm, ∫ₐᵇ f = F(b) − F(a) for <i>any</i> antiderivative, which is why the "+C" that annoys everyone in the indefinite integral is irrelevant in the definite one.'},
  {n:'An integral with no antiderivative', ex:'e^(−x²), and why that is fine', stage:'igFTC',
   opts:{key:'gauss', part:'two'},
   out:'Part 2 is unusable: no elementary antiderivative exists. Part 1 still guarantees one <i>does</i> exist — it is the accumulation function itself, and it is called erf. The integral is perfectly well defined and perfectly computable.',
   note:'Liouville proved the non-existence; it is not a failure of ingenuity. This is the clearest demonstration that the limit-of-sums definition is the real one and the antiderivative is a shortcut. The next stage but one computes this same integral exactly, by going up a dimension.'},
  {n:'What integrals accumulate', ex:'areas, volumes, lengths, work, averages', stage:'igApply',
   opts:{key:'between'},
   out:'One region, five different questions, five integrals — all built from the same move with a different quantity attached to each slice. The panel sums the drawn slices and compares with the exact integral.',
   note:'Cut the thing you want into slices small enough that each is approximately something you already know the size of, add them up, take the limit. The formula that comes out is never worth memorising; the slice is. Discs and shells answer different questions here because they revolve about different axes — the choice of axis decides the shape of the slice, and the slice decides the formula.'}
]},
{ g:'Double integrals', items:[
  {n:'Over a rectangle: the double Riemann sum', ex:'m × n columns, and Fubini', stage:'igDoubleRect',
   opts:{fn:'dome', m:6},
   out:'The columns drawn are the boxes the sum used, and the panel adds their volumes by summing them. The two iterated orders are computed independently and their difference printed — Fubini, verified rather than asserted.',
   note:'The double integral is a signed volume: where f is negative the columns hang below the base and subtract. Set f = 1 and it returns the area of the region, which is the trick that turns every area problem into an integral problem. For a separable integrand f = g(x)h(y) the whole thing factors into a product of two single integrals.'},
  {n:'Type I and Type II regions', ex:'the same set, swept two ways', stage:'igRegion',
   opts:{reg:'parab', fn:'sq', order:'dydx'},
   out:'The highlighted strip is one evaluation of the inner integral; the outer integral slides it across. Switch the order and the strips turn ninety degrees — and the two answers agree to quadrature precision.',
   note:'Inner limits may depend on the outer variable; outer limits must be constants, always. Swapping the order is often the difference between an integral you can do and one you cannot. The panel also runs a Monte Carlo estimate of the area that never sees the limit functions at all — a real check that the limits describe the region you think they do.'},
  {n:'A region with no Cartesian description', ex:'the cardioid, and the annulus', stage:'igRegion',
   opts:{reg:'cardio', fn:'one', order:'polar'},
   out:'The boundary of a cardioid is not the graph of anything, and an annulus would need four separate Cartesian integrals. In polar coordinates each is one line of algebra, and the strips become wedges.',
   note:'The area element is <b>r dr dθ</b>, not dr dθ. A wedge far from the origin is wider than the same wedge near it, in direct proportion to r — and the next stage measures exactly that.'},
  {n:'Write your own region — Type I, Type II or polar', ex:'x from a to b, y between two functions of x', stage:'igRegion',
   opts:{reg:'custom', regKind:'I', fn:'sq', order:'dydx'},
   out:'A region you describe rather than choose. The four kinds are the four ways of writing one down: <b>Type I</b> sweeps vertical strips, <b>Type II</b> horizontal ones written in <b>y</b>, <b>polar</b> sweeps wedges, and <b>both</b> takes all four limits at once. The bounding box is measured off the limit curves you type, not asked for, and a Monte Carlo estimate that never looks at a limit function checks the area independently.',
   note:'Every preset region here carries a Type I description, a Type II description and sometimes a polar one, and the stage makes a great deal of the difference between them — but until now a region the reader wrote could only ever be polar, which is the one case where the distinction does not arise. All four are typeable now. Wrap a square root in <b>max(0, …)</b> so it stays real at the ends of the interval; limits given the wrong way round are swapped and the panel says so; a limit that returns a non-finite value is counted and reported rather than silently read as zero. The order picker offers only the descriptions the region you typed actually has, because asking for the Type II integral of a Type I region would hand the quadrature a limit function that does not exist.'},
  {n:'Both descriptions at once, and Fubini tested', ex:'the two orders differenced, then refined', stage:'igRegion',
   opts:{reg:'custom', regKind:'both', fn:'one', order:'dydx'},
   out:'The same region given twice — y between x² and 2x over x ∈ [0,2], and x between y/2 and √y over y ∈ [0,4]. The panel integrates each way and prints the difference, then <b>refines the quadrature at 8, 16, 32 and 64 panels</b> and reports whether that difference shrinks. It does, by a large factor, which is Fubini holding.',
   note:'Fubini\'s theorem is normally something a textbook asserts and a reader believes. Here it is the one thing on the panel that can come out false. Two descriptions of the same set must give the same integral; two descriptions of <i>different</i> sets need not, and the way to tell them apart is not the size of the gap but its <b>behaviour under refinement</b>. A quadrature error falls when the panels are refined — the √y limit in the Type II description has an unbounded derivative at the origin and costs real accuracy, so the gap starts around 10⁻⁵ rather than at machine precision. A wrong limit does not fall at all. Change <b>y/2</b> to <b>y/3</b> and watch the verdict line change its mind while the numbers still look plausible.'},
  {n:'Where r dr dθ comes from', ex:'a polar cell is not a rectangle', stage:'igPolar',
   opts:{demo:'element'},
   out:'The exact area of a polar cell is ½(r₁²−r₀²)Δθ, which factors as (mean radius)·Δr·Δθ with no approximation. The cells tile the disc exactly and their areas total πR² at every resolution.',
   note:'Cells at twice the radius have twice the area for the same Δr and Δθ. That proportionality is the whole of the polar Jacobian — the extra r is not a fudge factor but the plain fact that the grid spreads out. Drop it and the panel shows you the number you get instead, which is not the area of anything.'},
  {n:'The Gaussian integral, in two dimensions', ex:'√π, by going up a dimension', stage:'igPolar',
   opts:{demo:'gauss'},
   out:'Square the integral, read the product as a double integral over the plane, switch to polar coordinates — and the stray r from the area element is <i>exactly</i> what the substitution u = r² needs. The panel does every step numerically alongside.',
   note:'The most famous integral in mathematics has no elementary antiderivative in one dimension and becomes trivial in two. Every normalisation constant in statistics, every Gaussian wave packet in the quantum wing, and every path integral in field theory rests on this one evaluation. It is Poisson\'s trick, and it is why √(2π) is everywhere.'}
]},
{ g:'Triple integrals and what they weigh', items:[
  {n:'Peeling a solid one variable at a time', ex:'surfaces, then curves, then numbers', stage:'igTriple',
   opts:{solid:'tetra'},
   out:'Each drawn ring is one cross-section, and the inner two integrals compute its area for each value of the outer variable. Every solid here has a known volume, and the nested quadrature reproduces it.',
   note:'The innermost variable runs between two <b>surfaces</b>, the middle one between two <b>curves</b> in the shadow the solid casts, the outer one between two <b>numbers</b>. Get that hierarchy right and the rest is bookkeeping. Setting f = 1 gives the volume — which is how the panel checks itself.'},
  {n:'Cylindrical and spherical volume elements', ex:'r dz dr dθ, and ρ² sin φ dρ dφ dθ', stage:'igCylSph',
   opts:{sys:'sph'},
   out:'Each drawn cell is coloured by its exact volume. The panel adds every cell up and compares the total with 4πρ³/3 — a check that the element is right at every resolution.',
   note:'Both factors are visible. The <b>ρ²</b> is the polar growth one dimension up: a shell twice as far out has four times the area. The <b>sin φ</b> is the shrinking of the θ-circles towards the poles — at φ = 0 a full turn in θ moves you nowhere, so cells there have no volume. Forgetting it over-counts the poles enormously, and the panel prints the ratio.'},
  {n:'The ice-cream cone, and why coordinates matter', ex:'six constant limits instead of a two-piece calculation', stage:'igTriple',
   opts:{solid:'icecream'},
   out:'A cone capped by a piece of sphere. In spherical coordinates all six limits are constants and the volume comes out as (2π/3)ρ³(1 − cos φ₀); in any other system it is a two-piece calculation.',
   note:'A coordinate system earns its keep when the boundary becomes a constant. This is the same lesson as the polar disc, one dimension up, and it is the practical reason the geometry wing bothers with cylindrical and spherical coordinates at all.'},
  {n:'Write your own solid — z-simple, cylindrical or spherical', ex:'ρ between two functions of φ and θ', stage:'igTriple',
   opts:{solid:'custom', solKind:'sph', sys:'sph'},
   out:'A solid you describe rather than choose, in whichever of the three systems its boundary is simplest in. The defaults are the ice-cream cone written directly: θ over a full turn, φ from 0 to π/4, ρ from 0 to 2. Set φ up to <b>pi</b> for the whole ball, or ρ from <b>1</b> to <b>2</b> for a shell. A Monte Carlo estimate that never looks at the limit functions checks the volume independently.',
   note:'A <b>z-simple</b> description — x between two numbers, y between two functions of x, z between two functions of x and y — can only describe a solid whose shadow is a Type I region. That is most solids and not all of them: the ice-cream cone here has no such description at all, which is why the preset carried a hand-written spherical block that the reader could look at and not edit. All three descriptions are typeable now. In <b>cylindrical</b> form the limits are θ, then r(θ), then z(r, θ), and the element is <b>r dz dr dθ</b> — the polar element with z carried along, which is why the system suits anything with an axis. In <b>spherical</b> form they are θ, then φ(θ), then ρ(φ, θ), and the element is <b>ρ² sin φ dρ dφ dθ</b>, whose two factors are the shell area growing as the square of the radius and the circles of latitude shrinking towards the poles. Choosing the system in which the boundary becomes a constant is the whole art of setting up a triple integral, and the only way to feel it is to write the same solid twice and see which one takes six constants and which takes two square roots.'},
  {n:'Mass, centroids and moments of inertia', ex:'the same machinery, different weights', stage:'igMass',
   opts:{reg:'tri', den:'linear'},
   out:'The cross-hairs mark where the plate would balance on a pin — and for an annulus that point is not inside the plate at all. The perpendicular-axis identity I₀ = I<sub>x</sub> + I<sub>y</sub> is checked, and so is the parallel-axis theorem.',
   note:'Moments weight the density by a coordinate; moments of <i>inertia</i> weight it by the square of a distance, and that squaring changes their character completely — a moment of inertia can never cancel, and mass far from the axis counts disproportionately. That is why a skater speeds up by pulling their arms in and why a flywheel is built wide.'},
  {n:'Change of variables — the Jacobian pays for the distortion', ex:'∬f dA = ∬f(T(u,v))|J| du dv', stage:'igChange',
   opts:{key:'ellip', fn:'one'},
   out:'Each cell is coloured by |J|, the factor its area is multiplied by on the way across. The panel integrates with the Jacobian and without it, and prints the error that omitting it would cause.',
   note:'Find a map that turns a nice region into your awkward one, pull the integrand back through it, and pay for the distortion with |J|. Polar coordinates are one line of this theorem. The absolute value is there because areas are unsigned — the sign of the determinant records whether the map turns the plane over, which in the one-variable substitution rule is handled invisibly by swapping the limits.'}
]}];

