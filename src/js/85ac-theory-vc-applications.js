const THEORY_VC_3 = `<h3 id="t9">The same ideas in two dimensions</h3>
<p>This tool offers three views of the same mathematics, and it is worth being precise about what changes between them.</p>
<h4>3D field</h4>
<p>The domain is a block of space. <span class="mth"><i>f</i></span> is a scalar at each point of that block, so its level sets are <em>surfaces</em>. Divergence is flux per unit <strong>volume</strong> through a closed <em>surface</em>. Curl is a genuine <em>vector</em>, because a loop can be oriented any way you like.</p>
<h4>2D map</h4>
<p>The domain is the <span class="mth"><i>xy</i></span>-plane and <span class="mth"><i>z</i></span> is ignored. Three things genuinely change:</p>
<ul>
  <li><strong>Level sets become curves.</strong> A contour map: lines of constant <span class="mth"><i>f</i></span>, labelled with their values, exactly like elevation on an Ordnance Survey sheet. The gradient still crosses every one of them at a right angle.</li>
  <li><strong>Divergence loses a term and changes dimension.</strong> <span class="mth">∇<span class="op">·</span><i>F</i> = ∂<i>P</i>/∂<i>x</i> + ∂<i>Q</i>/∂<i>y</i></span>. The closed surface becomes a closed <em>curve</em> and the volume becomes an <em>area</em>, so the flux integral <span class="mth">∯ <i>F</i><span class="op">·</span>n̂ <i>dS</i></span> becomes the line integral <span class="mth">∮ <i>F</i><span class="op">·</span>n̂ <i>ds</i></span>. The flux box becomes a flux <em>square</em> with four edges.</li>
  <li><strong>Curl becomes a single number.</strong> With only one plane available, only the <span class="mth">ẑ</span>-component survives:
    <span class="mth">∇<span class="op">×</span><i>F</i> = ∂<i>Q</i>/∂<i>x</i> − ∂<i>P</i>/∂<i>y</i></span>, a signed turning rate — positive for counter-clockwise. There is no normal vector to choose and nothing to sweep, so the probe draws a curved arrow instead of a straight one.</li>
</ul>
<p>Both of those 2D statements are halves of a single result:</p>
<div class="eqb"><span class="mth">∬<sub><i>D</i></sub> ( ∂<i>Q</i>/∂<i>x</i> − ∂<i>P</i>/∂<i>y</i> ) <i>dA</i> <span class="op">=</span> ∮<sub><i>∂D</i></sub> <i>P dx</i> <span class="op">+</span> <i>Q dy</i></span></div>
<p><strong>Green's theorem</strong> — Stokes' theorem in the plane. Its "flux form" is the 2D divergence theorem. The directional derivative is unchanged: still <span class="mth">∇<i>f</i><span class="op">·</span>û</span>, still the projection, still zero along a contour.</p>
<h4>Height map</h4>
<p>The domain is still the plane, but the third axis is used to <em>draw</em> the scalar as terrain: <span class="mth"><i>z</i> = <i>f</i>(<i>x</i>, <i>y</i>)</span>. Nothing about the mathematics changes — this is the 2D case wearing a costume — but the meaning becomes physical:</p>
<ul>
  <li><span class="mth">∇<i>f</i></span> is the compass bearing you would walk to go <strong>straight uphill</strong>, and <span class="mth">|∇<i>f</i>|</span> is how steep that is. It lies flat in the map plane; it is not a vector pointing up the hill in 3D.</li>
  <li><span class="mth"><i>D</i><sub>û</sub><i>f</i></span> is literally the <strong>slope</strong> of the ground along a chosen bearing — the tool draws the rise-over-run triangle on the surface itself. Walk along a contour and the rise is zero.</li>
  <li>Level sets become the <strong>contour rings</strong> of the terrain, each sitting at exactly its own height.</li>
  <li><span class="mth">∇²<i>f</i></span> tells you whether you are in a bowl (positive) or on a dome (negative).</li>
</ul>
<p class="note">One thing to watch: a height map is a picture of a <em>scalar</em> field of two variables. It cannot draw a 3D scalar field, and it is not the graph of a vector field. When you switch a vector field into height view, the surface shows a scalar built from it — its divergence or its magnitude — chosen with the slice control.</p>

<h3 id="t10">Where these are actually used</h3>
<h4>Maxwell's equations</h4>
<p>All four are statements about the divergence and curl of two vector fields. They are the reason this vocabulary exists at all.</p>
<div class="eqb"><span class="mth">∇<span class="op">·</span><i>E</i> <span class="op">=</span> <span class="frac"><span class="nm">ρ</span><span class="den">ε<sub>0</sub></span></span> &nbsp;&nbsp;&nbsp; ∇<span class="op">·</span><i>B</i> <span class="op">=</span> 0 &nbsp;&nbsp;&nbsp; ∇<span class="op">×</span><i>E</i> <span class="op">=</span> <span class="op">−</span><span class="frac"><span class="nm">∂<i>B</i></span><span class="den">∂<i>t</i></span></span> &nbsp;&nbsp;&nbsp; ∇<span class="op">×</span><i>B</i> <span class="op">=</span> μ<sub>0</sub><i>J</i> <span class="op">+</span> μ<sub>0</sub>ε<sub>0</sub><span class="frac"><span class="nm">∂<i>E</i></span><span class="den">∂<i>t</i></span></span></span></div>
<ul>
  <li><strong>Gauss's law.</strong> <span class="mth">∇<span class="op">·</span><i>E</i> = ρ/ε<sub>0</sub></span> — electric charge is the source of <span class="mth"><i>E</i></span>. Integrate over a region and apply the divergence theorem and you get the schoolbook form: the flux through any closed surface equals the enclosed charge over <span class="mth">ε<sub>0</sub></span>. Try the <strong>point charge</strong> example: the divergence is zero everywhere you can put the box, because all the charge is at the one point the field is undefined.</li>
  <li><strong>No magnetic monopoles.</strong> <span class="mth">∇<span class="op">·</span><i>B</i> = 0</span> — every magnetic field line closes on itself. Load the <strong>dipole</strong> and put the flux box anywhere: always zero, never a source. This is why cutting a magnet in half gives two magnets, not a north pole and a south pole.</li>
  <li><strong>Faraday's law.</strong> <span class="mth">∇<span class="op">×</span><i>E</i> = −∂<i>B</i>/∂<i>t</i></span> — a changing magnetic field makes the electric field circulate. Apply Stokes and the circulation around a loop is the induced EMF: generators, transformers and induction hobs are all this equation.</li>
  <li><strong>Ampère–Maxwell.</strong> <span class="mth">∇<span class="op">×</span><i>B</i> = μ<sub>0</sub><i>J</i> + …</span> — current makes <span class="mth"><i>B</i></span> circulate around it. The <strong>wire</strong> example is exactly this: zero curl in the empty space around the wire, but non-zero circulation for any loop that encircles it.</li>
</ul>
<p>Taking the curl of the last two and using <span class="mth">∇<span class="op">×</span>(∇<span class="op">×</span><i>F</i>) = ∇(∇<span class="op">·</span><i>F</i>) − ∇²<i>F</i></span> gives the wave equation in empty space, with speed <span class="mth">1/√(μ<sub>0</sub>ε<sub>0</sub>)</span> — which turned out to be the speed of light. Light is a consequence of these two operators.</p>
<h4>Fluids</h4>
<ul>
  <li><strong>Conservation of mass</strong> is a divergence statement: <span class="mth">∂ρ/∂<i>t</i> + ∇<span class="op">·</span>(ρ<i>v</i>) = 0</span>. Whatever leaves a region has to come out of what was stored there.</li>
  <li><strong>Incompressibility</strong> is <span class="mth">∇<span class="op">·</span><i>v</i> = 0</span>. Water essentially satisfies this, which is why the flux box in the whirlpool example always balances.</li>
  <li><strong>Vorticity</strong> is <span class="mth">ω = ∇<span class="op">×</span><i>v</i></span>: the local spin of a fluid element. It is what the paddle wheel measures, what makes aircraft wingtip vortices and bathtub drains, and it is conserved in ideal flow.</li>
</ul>
<h4>Heat, and everything shaped like heat</h4>
<p><strong>Fourier's law</strong> says heat flows down the temperature gradient: <span class="mth"><i>q</i> = −<i>k</i>∇<i>T</i></span>. Combine that with conservation of energy — a divergence statement again — and you get the <strong>heat equation</strong> <span class="mth">∂<i>T</i>/∂<i>t</i> = α∇²<i>T</i></span>. The same shape governs diffusion of a chemical, the Black–Scholes equation in finance, and blurring in image processing. In every case the Laplacian is comparing a point with the average of its neighbours and pulling it towards them.</p>
<h4>Antennas, superposition, and steering a beam with arithmetic</h4>
<p>Because Maxwell's equations are linear, fields <strong>add</strong>. That single fact — superposition — is what the physics builder demonstrates: a dipole is <span class="mth"><i>E</i><sub>+</sub> + <i>E</i><sub>−</sub></span>, two wires are two Ampère fields summed, and an antenna array is nothing more exotic than</p>
<div class="eqb"><span class="mth"><i>E</i><sub>z</sub>(<b>x</b>, <i>t</i>) <span class="op">=</span> Σ<sub><i>i</i></sub> <span class="frac"><span class="nm">cos(<i>k</i>|<b>x</b> − <b>x</b><sub><i>i</i></sub>| − <i>t</i> + φ<sub><i>i</i></sub>)</span><span class="den">√|<b>x</b> − <b>x</b><sub><i>i</i></sub>|</span></span></span></div>
<p>— one outgoing ripple per element. Where the ripples arrive <em>in phase</em> they reinforce; a quarter-wave out of step, they cancel. A constant phase step <span class="mth">Δφ</span> between neighbours tilts the surfaces of agreement, so the main lobe swings to the angle <span class="mth">sin θ = Δφ/(<i>k d</i>)</span> — the array steers <strong>without anything moving</strong>. That is a phased-array radar and a 5G panel in one line of trigonometry. (On screen the 1/√ρ amplitude is softened right at each mast so the singularity cannot blow up the colour scale; everywhere else the field is the honest sum.)</p>

<h4>Gradients outside physics</h4>
<ul>
  <li><strong>Optimisation and machine learning.</strong> Training a neural network is gradient descent: compute <span class="mth">∇<i>L</i></span> of the loss with respect to millions of parameters and step along <span class="mth">−∇<i>L</i></span>. Backpropagation is the chain rule, applied at scale — the same rule the derivation panel is using on your expression.</li>
  <li><strong>Terrain and navigation.</strong> The gradient is the fall line a skier follows; the directional derivative is the grade of a road on a chosen bearing; contour lines on a map are level sets.</li>
  <li><strong>Economics.</strong> A directional derivative is a marginal rate — how much output changes per unit of a particular input, holding the mix fixed.</li>
  <li><strong>Image processing.</strong> Edge detection computes <span class="mth">|∇<i>I</i>|</span> of image brightness: edges are exactly where the gradient is large, and its direction is perpendicular to the edge.</li>
</ul>

<h3 id="t11">Optimization — the whole toolkit at work</h3>
<p>Finding the minimum of a function is the single most consequential application of these operators. Each one plays a distinct role, and the <strong>Gradient descent</strong> panel lets you run all of it live.</p>
<h4>Gradient: the descent direction and the stopping rule</h4>
<p>Among all unit directions, <span class="mth"><i>D</i><sub>û</sub><i>f</i> = |∇<i>f</i>| cos θ</span> is most negative at <span class="mth">û = −∇<i>f</i>/|∇<i>f</i>|</span> — straight downhill is provably the locally fastest way down (that is Cauchy–Schwarz, nothing more). Gradient descent iterates</p>
<div class="eqb"><span class="mth"><b>x</b><sub><i>k</i>+1</sub> <span class="op">=</span> <b>x</b><sub><i>k</i></sub> <span class="op">−</span> η ∇<i>f</i>(<b>x</b><sub><i>k</i></sub>)</span></div>
<p>and stops where <span class="mth">∇<i>f</i> = <b>0</b></span>, the <strong>first-order condition</strong>: at an interior minimum no direction is downhill, so every directional derivative is ≥ 0, so the gradient has nowhere to point. Training a neural network is exactly this loop with millions of coordinates; backpropagation is the chain rule computing <span class="mth">∇<i>f</i></span> — the same rule the derivation panel applies to your expression.</p>
<p>The step size <span class="mth">η</span> (the <em>learning rate</em>) is a genuine trade-off, not a nuisance: on the bowl <span class="mth"><i>f</i> = <i>x</i>² + <i>y</i>²</span> the update is exactly <span class="mth"><b>x</b> ← (1 − 2η)<b>x</b></span>, so <span class="mth">η &lt; ½</span> glides, <span class="mth">½ &lt; η &lt; 1</span> zigzags but converges, and <span class="mth">η &gt; 1</span> diverges. The overshoot example lets you feel all three regimes with one slider.</p>
<h4>Directional derivative: descent directions and constraints</h4>
<p>A direction <span class="mth">û</span> is a <strong>descent direction</strong> exactly when <span class="mth"><i>D</i><sub>û</sub><i>f</i> &lt; 0</span> — the cool lobe of the rose diagram is precisely the set of them. Line-search methods pick a direction from that lobe, walk until it stops paying, and repeat.</p>
<p>Under a constraint <span class="mth"><i>g</i>(<b>x</b>) = <i>c</i></span> you may only walk along directions tangent to the constraint set. At a constrained optimum, none of those tangent directions is a descent direction, so <span class="mth">∇<i>f</i></span> must be perpendicular to the whole tangent plane — which is to say parallel to <span class="mth">∇<i>g</i></span>:</p>
<div class="eqb"><span class="mth">∇<i>f</i> <span class="op">=</span> λ ∇<i>g</i></span></div>
<p><strong>Lagrange multipliers</strong>, read geometrically: at the optimum the level set of <span class="mth"><i>f</i></span> and the constraint curve <em>kiss</em> — they touch without crossing, because crossing a level set means the value is still changing. You can see the ingredients on any contour map: the two families of curves and the two gradients, forced parallel at the extremum.</p>
<h4>Divergence and the Laplacian: what kind of critical point, and how the flow behaves</h4>
<p>Where <span class="mth">∇<i>f</i> = 0</span>, first-order information is exhausted; the Hessian decides. Positive-definite (curving up every way) means a minimum, negative-definite a maximum, mixed signs a saddle — the saddle example shows why optimizers slow to a crawl near such points: the gradient is genuinely tiny there. The Laplacian, the Hessian's trace, gives the quick test on screen: <span class="mth">∇²<i>f</i> &gt; 0</span> is necessary at a minimum, <span class="mth">&lt; 0</span> at a maximum. Newton's method goes one step further and preconditions the gradient by the inverse Hessian, <span class="mth"><b>x</b> ← <b>x</b> − <i>H</i>⁻¹∇<i>f</i></span>, which is why it laughs at ill-conditioned bowls that make plain descent zigzag.</p>
<p>Divergence enters through the <strong>descent flow</strong> <span class="mth">ẋ = −∇<i>f</i></span>, the continuous limit of the algorithm. Its divergence is</p>
<div class="eqb"><span class="mth">∇<span class="op">·</span>(−∇<i>f</i>) <span class="op">=</span> −∇²<i>f</i></span></div>
<p>so around a minimum (<span class="mth">∇²<i>f</i> &gt; 0</span>) the descent field has <em>negative</em> divergence: <strong>minima are the sinks of the descent flow</strong>, maxima its sources. Switch to scalar mode, turn on flow lines, and watch every streamline of the bowl drain into the bottom — the volume of a blob of starting points contracts at exactly the rate <span class="mth">−∇²<i>f</i></span>.</p>
<h4>Momentum: descent with memory</h4>
<p>Plain descent ricochets in narrow valleys: steep walls dominate <span class="mth">∇<i>f</i></span>, so the step points mostly <em>across</em> the valley rather than along it. The heavy-ball fix keeps a running velocity,</p>
<div class="eqb"><span class="mth"><b>v</b> ← β<b>v</b> <span class="op">−</span> η∇<i>f</i> , &nbsp;&nbsp; <b>x</b> ← <b>x</b> <span class="op">+</span> <b>v</b></span></div>
<p>The crosswise components alternate in sign and cancel out of the average, while the along-valley components always agree and accumulate. Run the <strong>race</strong> demo: same start, same step size, and the momentum walker simply drives past. Every modern training optimizer — Adam, RMSProp, Nesterov — is this idea plus bookkeeping. The failure mode is also visible: push β toward 1 and the ball overshoots the minimum and swings like a pendulum.</p>
<h4>Basins of attraction: optimization as a map</h4>
<p>A single run of descent answers "where does <em>this</em> start end up?" The basin map answers it for <em>every</em> start at once: colour each point of the plane by the minimum its descent reaches. The coloured regions are the basins; their borders are the ridge lines where <span class="mth">∇<i>f</i></span> is torn between two valleys. Two honest lessons fall out immediately: the deepest minimum need not own the biggest basin, and starts near a border are wildly sensitive — a nudge changes the answer entirely. This is why non-convex training is run from many random initialisations, and why "the optimizer found <em>a</em> minimum" is a much weaker claim than it sounds.</p>
<h4>Constraints, once more with machinery</h4>
<p>The Lagrange picture from above becomes an algorithm: <strong>projected gradient descent</strong>. Step downhill, then project the step onto the constraint set — on the circle, simply keep the tangential component and renormalise. The walk can only halt where the tangential shadow of <span class="mth">−∇<i>f</i></span> vanishes, i.e. where <span class="mth">∇<i>f</i> = λ∇<i>g</i></span>, and the demo prints the λ it finds. That number is not decoration: it is the <em>shadow price</em> — the rate at which the optimal value would improve if the constraint were loosened — which is why the same symbol runs through economics, control and engineering design.</p>
<h4>Least squares: the oldest optimization there is</h4>
<p>Fitting a line <span class="mth">y = a x + b</span> by minimising the mean squared error makes the loss</p>
<div class="eqb"><span class="mth"><i>L</i>(<i>a</i>, <i>b</i>) <span class="op">=</span> <span class="frac"><span class="nm">1</span><span class="den"><i>N</i></span></span> Σ<sub><i>i</i></sub> ( <i>y</i><sub><i>i</i></sub> <span class="op">−</span> <i>a x</i><sub><i>i</i></sub> <span class="op">−</span> <i>b</i> )²</span></div>
<p>a paraboloid over parameter space — the exact surface the fit demo walks on, with the inset translating each position back into a candidate line. Setting <span class="mth">∇<i>L</i> = 0</span> gives the normal equations and a closed-form answer; descent finds the same point iteratively. The reason this demo matters is the substitution it invites: replace the 2-parameter line with a 10⁹-parameter network and <em>nothing conceptual changes</em> — a loss over parameters, a gradient from the chain rule, a walker stepping downhill. The convexity is what you lose, which is exactly where the basin map and the saddle demo pick the story back up.</p>
<h4>Curl: the obstruction — when there is nothing to descend</h4>
<p>Descent presupposes a potential: dynamics <span class="mth"><i>F</i></span> are gradient descent on something only if <span class="mth"><i>F</i> = −∇<i>f</i></span> for some <span class="mth"><i>f</i></span>, and (on a simply connected domain) that happens exactly when <span class="mth">∇<span class="op">×</span><i>F</i> = <b>0</b></span>. <strong>Curl is the precise measure of how far your dynamics are from being an optimization at all.</strong></p>
<p>This is not a technicality. Two players running simultaneous gradient steps on the game <span class="mth">min<sub><i>x</i></sub> max<sub><i>y</i></sub> <i>x</i>·<i>y</i></span> generate the joint dynamics <span class="mth">(−<i>y</i>, <i>x</i>)</span> — pure rotation, curl 2, divergence 0. There is no function both steps descend, so the pair orbits forever; with finite steps it even spirals slowly outward. This is the classic instability of GAN training, and modern fixes amount to adding a contracting component — negative divergence — to the game dynamics. The Helmholtz decomposition says any smooth dynamics splits into exactly those two ingredients: a gradient part that optimizes and a rotational part that circulates. The two operators on this page measure the two parts.</p>

`;
