const SIGNAL_GROUPS = [
{ g:'Sampling — what a rate can and cannot carry', items:[
  {n:'A tone below Nyquist, and the samples that determine it', ex:'fₛ > 2B, and nothing is lost', stage:'sigAlias',
   opts:{skey:'tone', fs:32},
   out:'Three cycles a second sampled thirty-two times a second: the reconstruction built from the dots alone lies on the original to four parts in a thousand, and the 3 Hz dot on the folding map sits on the diagonal — the part of the map where a frequency appears where it is.',
   note:'The residual is not zero and the reason is worth knowing before anything else here. Whittaker–Shannon sums over <b>every</b> sample from minus infinity to plus infinity; a finite record truncates that sum, and sinc decays only as 1/t, so what is left is worst at the ends. The panel doubles the record internally and reports what that does to the error — here it halves it. That is truncation, and the next demo shows an error that does the opposite.'},
  {n:'Above Nyquist: a residual that does not respond to more data', ex:'19 Hz sampled at 32 comes back as 13', stage:'sigAlias',
   opts:{skey:'hi', fs:32},
   out:'The same 32 samples a second, and a tone at 19 Hz instead of 3. The bright reconstruction locks onto a completely different, slower wave at 13 Hz — |19 − 32| — the dot on the folding map sits on the fold rather than the diagonal, and doubling the record changes the residual by a factor of 1.00 rather than halving it.',
   note:'The diagnosis is the whole point. The same number — "the reconstruction differs from the signal by 2.0" — means two opposite things depending on how it responds to more data. A truncated sum halves; a folded frequency does not move, because nothing is missing from the sum. <b>Halve the step, double the record, and watch</b> is how the two are told apart, here and everywhere else in this laboratory.'},
  {n:'Two tones, one rate: the survivor and the casualty', ex:'3 Hz kept, 11 Hz folded to 5', stage:'sigAlias',
   opts:{skey:'two', fs:16},
   out:'At sixteen samples a second the 3 Hz component is below Nyquist and comes through untouched, while the 11 Hz one folds to 5 Hz — a frequency that was never in the signal at all. Both dots appear on the folding map, one on the diagonal and one on the fold.',
   note:'This is why aliasing is dangerous rather than merely inaccurate. The output is not noisy and not obviously wrong: it is a clean 5 Hz tone that a spectrum analyser will report with confidence. Nothing downstream can distinguish it from a 5 Hz tone that was really there, because the two have <b>identical samples</b> — not similar ones.'},
  {n:'A carrier at exactly Nyquist can sample to nothing at all', ex:'sin(2π·12t) at fₛ = 24', stage:'sigAlias',
   opts:{skey:'am', fs:24},
   out:'Every sample of the carrier is exactly zero, so the record is the sidebands and nothing else. The reconstruction shows a 1 Hz wobble with no carrier under it.',
   note:'The sampling theorem asks for f<sub>s</sub> <b>strictly greater</b> than 2B, and this is what the strictness is for. A sinusoid sampled at exactly twice its frequency is caught at the same phase every time — here the zero crossings. Shift the phase and you catch the peaks instead and read full amplitude. The theorem cannot hold at the endpoint because the answer there depends on something the theorem does not mention.'},
  {n:'A square wave has no rate that is fast enough', ex:'harmonics at 2, 6, 10, 14 … falling only as 1/k', stage:'sigAlias',
   opts:{skey:'square', fs:32},
   out:'5.0% of its energy is above Nyquist at 32 samples a second, 6.7% at 24, and the number never reaches zero however far you push the rate, because the harmonics never stop. The panel stops asking "does it alias" and reports how much.',
   note:'This is the ordinary case and the two before it were the exception. Almost nothing in nature is band-limited; what is band-limited is what comes out of the filter in front of the converter. That is why the next demo exists and why every digitiser has one.'},
  {n:'The cure, and it has to happen before the sampler', ex:'low-pass first, then sample', stage:'sigAlias',
   opts:{skey:'chirp', fs:24, guard:true},
   out:'Without the guard, 11.0% of the chirp is above Nyquist and the top of the sweep comes back DOWN the picture while the true signal is still going up. With it that falls to 0.34% — a factor of 32 — and the panel reports how much of the signal was spent buying it.',
   note:'Turn the checkbox on and off and read the two panels. Two things are worth noticing and the second is the one people skip. The filter cannot recover anything: it <b>removes</b> what cannot be represented, before it becomes indistinguishable from what can. And the figure it leaves is not zero, because a real filter has a transition band — what survives is content just above Nyquist, where the filter is still on its way down to the stopband. Both are why aliasing is preventable in hardware and unfixable in software.'},
  {n:'Type your own signal, and have its band limit measured', ex:'anything in t — the panel finds where it stops', stage:'sigAlias',
   opts:{skey:'custom', fs:32},
   out:'Whatever you write, the panel reports the frequency under which all but a ten-thousandth of its energy sits, the fraction that a given rate would fold, and whether the reconstruction residual responds to a longer record. Nothing is declared and nothing is assumed.',
   note:'Try <b>exp(-4*t)*sin(2*pi*9*t)</b>: a decaying tone, whose spectrum is a Lorentzian and therefore has no band limit at all — the measured edge moves as you change what "all but" means. Then try <b>sin(2*pi*7*t)</b> and watch it stop moving. A band limit is a property some signals have and most do not.'}
]},
{ g:'Windows — the price of looking at a finite piece', items:[
  {n:'A tone on a bin, and the same tone half a bin off', ex:'one clean line, or a smear across the whole axis', stage:'sigWindow',
   opts:{wkey:'rect', N:256, off:0},
   out:'On a bin the rectangle gives one line and nothing else — the sidelobes are exactly zero. Drag the offset to 0.5 and the same tone smears across every bin in the picture, with the nearest reading 3.92 dB low.',
   note:'Nothing changed about the signal. What changed is whether a whole number of cycles fits in the record — and the DFT represents the signal that <b>repeats</b> those samples forever, so if it does not fit, the repetition has a step in it and a step has content everywhere. The zeros in the first case are not the absence of leakage; they are the Dirichlet kernel\'s nulls landing exactly on the other bins.'},
  {n:'The window\'s own transform, in closed form and by FFT', ex:'W(δ) = Σ(−1)ᵏ(aₖ/2)[D(δ−k) + D(δ+k)]', stage:'sigWindow',
   opts:{wkey:'hann', N:256, off:0.5},
   out:'Two curves drawn on top of one another — a thick pale one and a thin bright one — agreeing to about a part in 10¹⁴ over ninety decibels of range.',
   note:'The bright curve is an FFT of the windowed samples. The pale one never forms the window at all: it adds up shifted Dirichlet kernels, one pair per cosine term, using only the four coefficients that define the window. Two routes with nothing in common, and a mistyped coefficient could not survive the comparison.'},
  {n:'Coherent gain and noise bandwidth, summed and derived', ex:'Σw/N = a₀, and N·Σw²/(Σw)² = 1 + Σaₖ²/2a₀²', stage:'sigWindow',
   opts:{wkey:'blackman', N:256, off:0.25},
   out:'Both identities hold to the last bit: 0.42 exactly for Blackman\'s coherent gain, and 1.7267573696 bins for its noise bandwidth, from two routes that share no arithmetic.',
   note:'These are identities rather than approximations, and the reason is one line: over a whole number of periods every cosine sums to zero, and every product of two different cosines does too. So Σw is a₀N and Σw² is N(a₀² + ½Σaₖ²), with nothing left over. Hann gives exactly 3/2; the flat top gives 3.77, which is what "flat top" costs.'},
  {n:'The flat top: built to measure amplitude, not frequency', ex:'scalloping loss under 0.01 dB, main lobe five bins wide', stage:'sigWindow',
   opts:{wkey:'flattop', N:256, off:0.5},
   out:'Half a bin off centre, the rectangle under-reads the amplitude by 3.92 dB and the flat top by 0.0098 dB. Its price is on the row above: a first null five bins out instead of one.',
   note:'The window a calibration laboratory uses, and it looks absurd until you know what it is for. If you need to know how <i>loud</i> a tone is and it will not conveniently sit on a bin, everything else is a source of error up to four decibels wide. If you need to know <i>where</i> it is, this is the worst window in the picker. There is no best; there is only what you are measuring.'},
  {n:'Two tones close together: a resolution limit', ex:'a wide main lobe merges them however clean it is', stage:'sigWindow',
   opts:{wkey:'blackman', N:256, off:0, two:true, sep:2, a2:0.5},
   out:'Two bins apart, Blackman shows one lump — its main lobe is 3.4 bins wide and it physically cannot separate them. Switch to the rectangle and two peaks appear, sidelobes and all.',
   note:'The first of two quite different failures, and the picker separates them. This one is about <b>width</b>: no amount of sidelobe suppression helps, and the only cure is a longer record, because a bin is f<sub>s</sub>/N and that is the only place N appears.'},
  {n:'A quiet tone beside a loud one: a dynamic-range limit', ex:'high sidelobes bury it however far away it is', stage:'sigWindow',
   opts:{wkey:'rect', N:256, off:0.3, two:true, sep:6, a2:0.003},
   out:'A tone 50 dB down and six bins away is completely invisible under a rectangle — its skirts are still 30 dB up at that distance. Switch to Blackman–Harris and it appears, unmistakably, with 40 dB to spare.',
   note:'The opposite failure, and the opposite cure. Here the tones are far apart and a longer record does not help at all; what is needed is a window whose transform decays faster. This is why a spectrum analyser has a window setting and why leaving it on the default is how quiet things go unnoticed.'},
  {n:'Every window in the picker, and none of them wins', ex:'main lobe against sidelobes, seven times', stage:'sigWindow',
   opts:{wkey:'hann', N:256, off:0.5, db:-120},
   out:'Rectangular: 1 bin, −13 dB. Hann: 2 bins, −31 dB. Blackman: 3 bins, −58 dB. Blackman–Harris: 4 bins, −92 dB. Flat top: 5 bins, and a scalloping loss a hundred times smaller than any of them.',
   note:'Step through the picker and watch one number improve as the other gets worse, every time and without exception. The reason is not empirical: a window\'s transform decays fast exactly when the window is smooth, and a smooth window that reaches zero at both ends has to spend more of its length getting there. Width and decay are the same property seen twice.'}
]},
{ g:'Filters — the thing you actually build', items:[
  {n:'A moving average is a low-pass, and a poor one', ex:'|H| = |sin(8πf) / (8 sin πf)|', stage:'sigFilter',
   opts:{fkey:'avg', view:'mag'},
   out:'Zeros exactly at 1/8, 2/8, 3/8 of the sample rate — where a whole number of cycles fits the window — and sidelobes 13 dB down that never improve. Its impulse response below is eight equal spikes, which is the whole filter.',
   note:'The Dirichlet kernel again, in its third role in this wing: it was the DFT of a rectangle, then the rectangular window\'s leakage, and now the frequency response of a rectangular impulse response. They are one function, and noticing that is worth more than any of the three separately. An average is cheap, linear phase, and about as good a low-pass as a rectangle is a window.'},
  {n:'The response, from the coefficients and by running the filter', ex:'B(z)/A(z) against e^(2πifn) driven through', stage:'sigFilter',
   opts:{fkey:'lp', view:'mag'},
   out:'Two curves that are one curve — the worst disagreement across the whole band is about 5×10⁻¹⁵ against a peak gain of 1.0. For an FIR this is exact rather than close: after 40 samples the transient is over and there is nothing left to differ.',
   note:'The second route is not a numerical check on the first, it is a different question with the same answer. One evaluates a rational function on the unit circle; the other runs the difference equation on a complex exponential and divides. That they agree is the theorem that a linear time-invariant system cannot change a frequency — and it is the reason the whole subject can be done with algebra.'},
  {n:'Linear phase, and what symmetric taps buy', ex:'τ = (M−1)/2 at every frequency, exactly', stage:'sigFilter',
   opts:{fkey:'lp', view:'gd'},
   out:'A flat line at 20 samples, held to a part in 10⁹ across the band — and the impulse response view shows why: reverse the 41 taps and you get the same 41 taps.',
   note:'A filter that delays every frequency equally moves a waveform without changing its shape; one whose delay varies smears a sharp edge into a ringing one. Symmetric taps force the phase to be a straight line, so the delay is its constant slope. This is the one thing an FIR can do that no IIR can, and it is why it is worth spending forty multiplications to do what two would nearly manage.'},
  {n:'Where the group delay does not exist', ex:'at a zero of H the phase jumps by π', stage:'sigFilter',
   opts:{fkey:'avg', view:'gd'},
   out:'Seven dashed lines across the picture, one at each zero of the moving average, and the panel counts them. Between them the delay is exactly 3.5 samples; at them there is no answer at all.',
   note:'Both routes refuse there, and the first version of this code did not. Differencing arg H across a jump of π and dividing by the step returned −24 999.5 samples for the difference filter at DC — a phase discontinuity wearing the units of a delay, finite, plausible and meaningless. A quantity defined as a derivative does not exist where the function is not differentiable, and printing something anyway is worse than printing nothing.'},
  {n:'An IIR: two multiplications, and it never quite forgets', ex:'y[n] = 0.1x[n] + 0.9y[n−1]', stage:'sigFilter',
   opts:{fkey:'one', view:'imp'},
   out:'An impulse response of 0.1×0.9ⁿ that decays forever without reaching zero, a DC gain of exactly 1, and a centre of mass at 9.000000 samples — which the panel checks against the group delay at DC computed from the phase.',
   note:'That identity is exact and worth its own line. Expand H(f) = Σh[n]e^(−2πifn) for small f: the phase is −2πf·Σnh[n]/Σh, so the delay at DC <i>is</i> a first moment — the same quantity a physicist would call a centre of mass. Two entirely different calculations, agreeing to every digit.'},
  {n:'A resonance is two poles, and a negative group delay', ex:'poles at 0.97·e^(±2πi·0.12)', stage:'sigFilter',
   opts:{fkey:'reso', view:'pz'},
   out:'Two crosses just inside the unit circle, a gain of exactly 1.000000 at their own frequency, and a group delay at DC of −0.888 samples: negative, and both routes agree on the sign.',
   note:'A negative group delay is not a violation of causality and this is the cleanest place to meet one. The gain is one over the distance to the poles, so pushing them towards the circle sharpens the peak without limit; the phase they produce runs the other way at DC. What is being advanced is the <i>envelope</i> of a signal the filter has already been hearing for a long time, and nothing can be advanced that has not started yet.'},
  {n:'Type your own coefficients, and find the poles', ex:'b and a, and the roots decide everything', stage:'sigFilter',
   opts:{fkey:'custom', view:'pz'},
   out:'Whatever two lists you write, the panel finds the roots of a by Aberth iteration, draws them on the unit circle, and reports the largest radius — with "stable" or "the output grows without bound" beside it rather than a number to interpret.',
   note:'Try <b>b = 1</b> with <b>a = 1, -1.9, 0.995</b>: poles at radius 0.9975 and a resonance so sharp the settling count climbs into the thousands. Then push the last coefficient past 1 and watch the verdict change. The root finder is the complex-numbers wing\'s, unmodified — stability is a question about where the roots of a polynomial are, and that question was already answered two wings ago.'}
]},
{ g:'Time and frequency at once', items:[
  {n:'A chirp, and the ridge that follows it', ex:'f(t) = f₀ + kt, measured against the picture', stage:'sigSpectro',
   opts:{skey:'chirp', fs:128, NW:64, wkey:'hann'},
   out:'A bright diagonal across the picture, and the loudest bin in each column tracks the law the signal was built from to a fortieth of a bin — 0.047 Hz against a bin spacing of 2 Hz.',
   note:'Two routes with nothing in common: one reads the peak of a transform of the samples, refined between bins by fitting a parabola to the <i>logarithm</i> of the magnitude; the other evaluates f₀ + kt. They are compared at the <b>centre</b> of each window, because a window sees the sweep cross it and reports the average — comparing against the frequency at the window\'s start would produce a residual of half a sweep and look like a broken estimator.'},
  {n:'Short window or long: the box changes shape and keeps its area', ex:'Δt·Δf = ENBW, whatever N is', stage:'sigSpectro',
   opts:{skey:'chirp', fs:128, NW:16, wkey:'hann'},
   out:'At N = 16 the diagonal is pinned to an eighth of a second and 12 Hz thick; at N = 256 it is 0.75 Hz thick and smeared over two whole seconds. The product of the two resolutions reads 1.5000000 at every setting on the slider.',
   note:'This is the uncertainty relation in the only form a computer ever sees, and it is a much cruder statement than σ<sub>t</sub>σ<sub>f</sub> ≥ 1/4π — which is precisely why it is more useful. The resolution cell drawn on the picture has a fixed area and every choice you make is a choice of its <b>shape</b>. It is the same fact as Δx·Δp ≥ ħ/2, with ħ replaced by a window you chose.'},
  {n:'The window is doing two jobs here, and they are the same job', ex:'the taper that controls leakage is the thing that says when', stage:'sigSpectro',
   opts:{skey:'chirp', fs:128, NW:64, wkey:'flattop'},
   out:'Switch from Hann to the flat top and the ridge thickens visibly: its noise bandwidth is 3.77 bins against Hann\'s 1.5, and the product on the panel jumps to match. The time resolution has not changed at all.',
   note:'In the window stage a window was a taper chosen to control leakage. Here it is also the thing that decides <i>when</i> — each column reports one window-length of signal. The two roles are one object, which is why the trade is unavoidable rather than an engineering compromise someone might one day solve.'},
  {n:'Two bursts: what one transform of the whole record cannot say', ex:'9 Hz, then 34 Hz — not both at once', stage:'sigSpectro',
   opts:{skey:'burst', fs:128, NW:64, hopDiv:4},
   out:'Two bright blobs at different heights and different times. A single transform of this record reports "9 and 34 Hz, both present", which is true and is the entire point being missed: they were never both present.',
   note:'This is the case the instrument exists for, and it is worth loading the same signal into the sampling stage to see what the other picture says about it. There, one spectrum of the whole record describes a signal that never existed — something humming at two frequencies for eight seconds. Here, the same data says what actually happened and in what order.'},
  {n:'What overlap is for, and what it is not for', ex:'hop = N/4, and the box does not change', stage:'sigSpectro',
   opts:{skey:'burst', fs:128, NW:64, hopDiv:1},
   out:'With no overlap a burst can fall between two columns and be tapered towards nothing by both. Drag the overlap up and it fills in — while the resolution box on the picture does not move a pixel.',
   note:'A common misreading, and the picture settles it. Overlap buys nothing in resolution: it cannot, because the resolution is set by the window length and that has not changed. What it buys is that no event falls in a gap. The cost is arithmetic, and the usual choice is a hop of a quarter of the window, which is where the slider starts.'},
  {n:'Type your own signal and watch where its energy goes', ex:'anything in t, laid out in time and frequency', stage:'sigSpectro',
   opts:{skey:'custom', fs:128, NW:64},
   out:'Whatever you write appears as a picture in both domains at once, with the ridge marking the loudest frequency in each column and the resolution cell showing exactly how much you are allowed to believe about either axis.',
   note:'Try <b>sin(2*pi*5*t) + sin(2*pi*20*t)*exp(-3*(t-4)^2)</b>: a steady tone with a burst partway through, which one transform of the whole record would describe as "5 and 20 Hz, both present" — true, and missing the entire point. This is the instrument that separates a signal whose content changes from one whose content does not, and it is the last thing this wing has to say about the difference.'}
]}
];
